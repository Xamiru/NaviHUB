# NaviHUB

Personal, single-user, local-only Electron media hub: anime, manga, visual novels, games, movies/TV (MyAnimeList + Letterboxd style tracking with cross-linked voice actors/studios/characters), plus standalone sections for Japanese learning, an English dictionary, a programming learn section, a local manga reader, a song quiz, and a local music library. No server, no accounts — everything lives in `~/.config/navihub/`.

## Commands

```bash
npm run dev          # electron-vite dev — DO NOT run from VS Code's integrated terminal (see below)
npm run test         # vitest via ELECTRON_RUN_AS_NODE=1 electron (better-sqlite3 is built for Electron's ABI; plain `npx vitest` fails)
npm run typecheck    # tsc for both tsconfig.node.json and tsconfig.web.json
npm run build        # production build to out/ (the user's desktop shortcut runs this output — rebuild after changes they should see)
npm run db:generate  # drizzle-kit (rarely needed; runtime schema comes from init.sql)
npm run pack:linux   # electron-builder packaged build to dist/linux-unpacked (packaged-mode smoke test)
npm run dist:win     # Windows NSIS installer + portable exe to dist/ (see scripts/dist-win.sh)
npm run export:library  # sanitized shareable library bundle (see scripts/export-library.cjs)
```

**Packaging (2026-07):** electron-builder config in `electron-builder.yml`. Windows cross-builds have two traps, both handled by `scripts/dist-win.sh`: NSIS uninstaller generation needs wine (so electron-builder runs inside the `electronuserland/builder:wine` docker image), and `@electron/rebuild` can't cross-fetch native modules for another OS — it silently keeps the Linux `better_sqlite3.node` (so the script swaps in the win32-x64 prebuild first; `npmRebuild` is off in the yml). After any Windows build, verify with `file dist/win-unpacked/resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node` → must say PE32+, not ELF. Two paths are asar-aware and must stay so: the kuromoji dict (tokenizer.ts, asarUnpack + `.replace('app.asar','app.asar.unpacked')`) and the window icon (index.ts, `process.resourcesPath` when packaged). `productName` lives ONLY in electron-builder.yml — adding it to package.json would change the userData folder name.

**CI/CD (2026-07-25):** `.github/workflows/release.yml` tests, packages and publishes on every push to `main` — installers come from GitHub Releases, not from a local build. The version is **derived, never committed**: `minor = BASE_MINOR + (git rev-list --count HEAD − BASE_COMMITS)`, with the two baselines as workflow `env` pinning "7 commits = 0.2.0", stamped in by `npm version --no-git-tag-version` at build time. So the committed `package.json` version is stale BY DESIGN, and 0.x grows forever (0.9.0 → 0.10.0). `actions/checkout` must keep `fetch-depth: 0` — a shallow clone makes the commit count 1 and every release gets the same version. Jobs: `verify` (typecheck + test; also the single source of the version, via a job output) → `build` matrix (`ubuntu-latest` → AppImage, `windows-latest` → NSIS + portable) → `release` (`gh release create`, re-run safe against an existing tag). **Windows builds natively**, which is why neither the wine image nor the win32 prebuild swap appears in the workflow — `npm ci`'s postinstall electron-rebuild produces a correct PE binary, asserted before upload by an MZ-magic check (the cross-platform form of the `file` check above). `scripts/dist-win.sh` is unchanged and remains the laptop-local path. In a build job NEVER reinstall node_modules after `npm ci` — postinstall would rebuild the native module. `SYNC_PROTOCOL_VERSION` is unrelated to the package version and must never be auto-bumped.

**In-app updates (2026-07-27):** Settings → System → Updates, backed by `electron-updater` against GitHub Releases. Three packaging facts are load-bearing: the `publish` block in electron-builder.yml is what makes electron-builder emit `latest.yml`/`latest-linux.yml` **and** embed `resources/app-update.yml` (verified: it still does under `--publish never`, which is why CI keeps publishing via `gh`); the workflow's matrix `files` MUST keep uploading those manifests or the updater has nothing to read; and `GITHUB_OWNER`/`GITHUB_REPO` in `updater.ts` duplicate that publish block deliberately — `setFeedURL` takes the whole config or nothing, and the token can only be supplied at runtime. **The repo is private**, so updating needs a `github.token` setting (`repo` scope / Contents:read) — wiped in sanitizeSql.cjs. `electron-updater` is a runtime **dependency** (externalized by `externalizeDepsPlugin`, so verify `grep 'require("electron-updater")' out/main/index.js`). Architecture mirrors musicDownload.ts: electron-updater is EventEmitter-based but this app has no push channel, so `updater.ts` collapses its events into ONE module-level status object polled via `update:status` (500ms while `checking`/`downloading`, self-gating off the polled data so a download survives navigation), with the `status.id !== id` stale-run guard and a **module-level** `lastSettled` in `lib/useUpdateStatus.ts`. Deliberately NOT `withActivity` — the shared slot clears on completion, but `ready`/`error` must stay readable after polling stops. `download()` is **fire-and-forget** (returns immediately; awaiting would hold the IPC reply open for the whole ~140MB transfer and the bar would never move). All decisions live in `updaterCore.ts` — pure, no SDK, so tests/updater.test.ts needs no electron (the coachTools.ts split). `UpdateEnvironment` is orthogonal to `state` and names the three builds that can't self-update: `dev` (not packaged), `portable` (the Windows portable target is unsupported by electron-updater — detected via `PORTABLE_EXECUTABLE_FILE`), `no-token`; they surface as an explanation, never an error. `killActiveUpdate()` is in the before-quit list. Also here: `desktopName` in package.json + `linux.syncDesktopName` set the Linux WM_CLASS so the window groups under the user's own `navihub.desktop` — that field is safe in package.json, unlike `productName`.

**Library export:** `scripts/export-library.cjs` snapshots the live DB, strips personal data via `scripts/sanitizeSql.cjs` (shared with tests/exportSanitize.test.ts — edit them together when tables gain personal columns), and stages `navihub.db` + `media/` + theme audio + README into `~/navihub-export/navihub-bundle`. New personal/tracking columns or tables MUST be added to sanitizeSql.cjs or they leak into exports.

**Terminal gotcha:** VS Code's integrated terminal sets `ELECTRON_RUN_AS_NODE=1`, which makes `npm run dev` silently run Electron as plain Node (no window). Launch the GUI from a normal terminal or the desktop shortcut, or `unset ELECTRON_RUN_AS_NODE` first.

**Git:** the user commits and pushes themselves. Never run git mutations; read-only status/log is fine.

## Architecture

electron-vite three-way split; path aliases `@shared` → `src/shared` (everywhere), `@` → `src/renderer/src` (renderer only).

- `src/main/` — all DB and filesystem work. Entry `index.ts` (window, `navimg://` protocol handler, before-quit hooks). Importers as one module per source (`anilist.ts`, `tmdb.ts`, `vndb.ts`, `rawg.ts`, `themes.ts`, `hltb.ts`). Feature modules (`manga.ts`, `music.ts`, `musicDownload.ts`, `musicArt.ts`, `mokuro.ts`, `jisho.ts`, `tokenizer.ts`). Queries in `repos/*.ts`.
- `src/preload/index.ts` — implements the `NaviApi` interface by forwarding every method to `ipcRenderer.invoke('domain:action', …)`.
- `src/renderer/src/` — React 18 + react-router v6 (HashRouter) + TanStack Query v5 + Tailwind (dark theme).
- `src/shared/` — `api.ts` (the typed `NaviApi` contract both preload and renderer import), `types.ts` (all IPC payload types), `mediaUrl.ts`, `srs.ts`.

### The IPC contract (follow this chain for ANY new backend capability)

1. Payload types in `src/shared/types.ts`.
2. Method signatures in the `NaviApi` group in `src/shared/api.ts`.
3. Thin `ipcMain.handle('domain:action', (_e, …) => repo.fn(…))` one-liners in `src/main/ipc.ts` (grouped by `// ---- domain ----` comments).
4. Mirror in `src/preload/index.ts`.
5. Renderer consumes via `api` (`lib/api.ts` = `window.api`) inside TanStack Query, keys from `lib/queryKeys.ts`.

Typecheck enforces preload/api agreement — if it passes, the contract is aligned.

**No push events exist** (no `ipcRenderer.on` bridge). Long-running main-process work (scans, downloads, art fetch) exposes a module-level status object behind a `domain:xStatus` invoke channel; the renderer polls it with `refetchInterval` while the operation's promise is pending. Keep this pattern. For **imports** specifically there's a shared global slot: `src/main/progress.ts` (`withActivity` wraps the import handlers in ipc.ts; `downloadImages` auto-reports per-image progress; importers mark `phase:'writing'` before their transaction) → polled via `activity:status` → `useActivity()`/`ActivityIndicator.tsx` (Topbar pill + ImportDialog bar). Instrument new importers the same way.

## Database

better-sqlite3 at `userData/navihub.db`. **Two schema files must be edited together:**

- `src/main/db/init.sql` — authoritative DDL, `CREATE TABLE IF NOT EXISTS`, runs on every startup.
- `src/main/db/schema.ts` — Drizzle mirror (types/drizzle-kit only; repos do NOT use the Drizzle query builder).

Repos use **raw prepared SQL** via `getSqlite()` from `db/connection.ts`. Columns added to an *existing* table also need an idempotent `ensureColumn` call in `runMigrations()` (connection.ts) — the live DB predates them; brand-new tables need nothing beyond init.sql. No FKs can be added by ALTER, so some link tables are deliberately FK-less (e.g. `list_item`, `jp_card.source_media_id`) — reads LEFT JOIN and tolerate deletion, and entity `remove()` fns clean up manually.

All media types share one `media_item` table keyed by a `media_type` text column. The cross-linking graph: `credit` (person ↔ media ↔ character), `media_company`, `media_character`, `tag`/`media_tag`, `media_relation`. Standalone sections have isolated tables (`jp_*`, `music_*`, `manga_chapter`, `list`/`list_item`, `theme_song`/`theme_artist`).

## Local files & images

Custom privileged `navimg://` protocol serves everything (images, audio, manga pages incl. streamed CBZ entries, EPUB spine XHTML/images). DB stores **relative paths with virtual prefixes**; only `src/main/files.ts:absoluteMediaPath()` maps prefix → real dir (and blocks `..`):

- `media/…` → `userData/media` (downloaded covers, content-addressed `dl-<sha1(url)>` via `downloadImage`)
- `audio/…` → `audio.dir` setting (anime theme songs)
- `manga/…` → `manga.dir` setting
- `music/…` → `music.dir` setting
- `pictures/…` → `pictures.dir` setting (wallpapers/fan art, human-readable layout `<Title> (<type>)/<wallpapers|fanart>/` via `downloadImageTo`/`copyImageInto` — deliberately NOT content-addressed; dedupe is per `media_image.source_url` row, filename clashes get ` (n)` suffixes)

Renderer builds URLs synchronously with `mediaUrl(relPath)` (no per-image IPC); `<img>` onError handles missing files (`CoverImage.tsx`). Settings are rows in the `settings` table (`settingsRepo.get/set`), surfaced in `SettingsPage.tsx`.

**Wallpapers/fan art (2026-07-09):** every media detail page gets a Wallpapers `MediaImagesSection`; non-movie/TV types also get Fan Art (`cfg.hasFanArt`). One `media_image` table (`kind` column), rows stripped on export (sanitizeSql.cjs). `src/main/pictures.ts` owns SQL + searches: Wallhaven (purity=100 SFW hardcoded — regression-tested) and TMDB backdrops (`tmdb.ts:fetchBackdrops`, movie/tv with `external_source='tmdb'` only). Searches go through IPC (renderer CSP has no `connect-src https:`; remote *thumbnails* are fine — `img-src https:`). `ImageBrowseDialog` per-tile busy/added state; `Lightbox.tsx` is the app's only fullscreen image modal (←/→ wrap). Removing an image deletes its file (pictures/ paths only); deleting media cascades rows but leaves files.

**EPUB light novels (2026-07-06)** live in the manga section as chapters: the scanner treats a `.epub` as one chapter whose `page_count` = spine length ("pages" = spine documents); `manga.ts:listChapterPages` is the format seam (folder → CBZ → EPUB). `src/main/epub.ts` parses container/OPF/nav/NCX with a hand-rolled, test-covered tag scan (no XML dep) behind an mtime cache; `archive.ts`'s `splitArchivePath` recognizes `.epub` segments so the protocol streams entries (EPUB_MIME adds xhtml/css/fonts — scripts never), but `.epub` is deliberately NOT in `ARCHIVE_EXTS` (a book must never scan as a CBZ of its embedded images). Renderer: `BookReaderPage` (`/manga/:id/book/:chapterId`, chrome-free) fetches spine XHTML via renderer `fetch()` (CSP `connect-src` includes `navimg:`), renders through `BookContent` — DOMParser + tag whitelist, NEVER dangerouslySetInnerHTML, img srcs resolved via the shared pure `resolveEpubHref` (`@shared/epubPaths`, also used by the OPF parser). All chapter navigation routes through `lib/readerPath.ts:readerPath()` (dirPath `.epub` suffix picks book vs image reader) so mixed series cross freely. Progress/read-state reuse `manga_chapter` unchanged (`last_read_page` = spine index); the reader reuses `MiningPanel` (tap paragraph → SRS) and persists typography in localStorage `book.readerPrefs`.

**Gacha section (2026-07-10)** — standalone tracker at `/gacha` for the live-service games the user plays (HSR/FGO/E7/WuWa): per-game roster (characters + the game's equipment kind), per-unit builds, currencies (inline-edit tiles), banner schedule, subreddit news. ALL per-game vocabulary lives in `src/shared/gacha.ts` (`GACHA_GAMES`: unit kinds, facet/dupes labels + `formatDupes`, currencies, `buildMode`, `subreddit`, `color` hex accent) — the `gacha_*` tables are game-agnostic, so adding a game = one config entry. Kind/currency `key` strings are FROZEN (stored in DB, part of the future catalog importers' `(game, kind, external_source, external_id)` unique dedup key). `gacha_unit.dupes` is 0-based copies-consumed (HSR eidolon, FGO NP-1, E7 imprint, WuWa sequence) — never write 1-based values. FGO is `buildMode:'levelOnly'` → no Builds UI anywhere. **Online fetching is button-only, never automatic.** News = each game's subreddit **hot Atom feed** (`r/<sub>/hot.rss`) — Reddit's unauthenticated JSON API 403s ALL non-browser clients (verified: www/old/api hosts, any UA), only RSS is open, and it rate-limits bursts with 429 + NO retry-after; the fetcher passes `rateLimitWaits: 0` (opt-out added to `fetchWithRetry`) and fails fast with a toast instead of hanging the button 60s. `parseSubredditFeed` is a hand-rolled fixture-tested Atom tag-scan (epub.ts approach). A fetch REPLACES the game's cached feed (`gacha_news.sort_order` = hot rank; empty parse throws before touching the cache); `author`+`sort_order` arrived post-ship → ensureColumn in connection.ts. Per-game hero art (hub cards + dashboard header banners) lives in `gacha_meta 'image'` via `gacha:setGameImage`, picked in `GachaGameImageDialog` — fastest path reuses a games-section RAWG cover (`api.media.list` game search), else file/URL. News thumbnails stay remote https URLs (CSP `img-src` allows it); unit/banner/hero art downloads via `gacha:downloadImage` → content-addressed `media/`. News links open through `app:openExternal` (http/https-guarded; `createWindow` denies all `window.open`) — never raw `<a href>` (HashRouter hijacks). Every `gacha_*` table is personal → wiped in sanitizeSql.cjs; keep any new gacha table added there. Detail phases planned per game (E7 Fribbels-style gear optimizer w/ future `gacha_gear` table, catalog importers with `owned=0` rows, HSR warp history) — all bolt on without destructive migration.

**FGO Coach (2026-07-13)** — the first per-game gacha detail phase: an LLM coaching chat at `/gacha/fgo/coach`, gated by `GachaGameCfg.coach` (fgo only — flip the flag for others). FIRST LLM feature in the app. **Provider is switchable** (`coach.provider` setting, DEFAULT `gemini`): **Google Gemini via `@google/genai`** (`gemini.api_key` — free AI Studio key, no card; the user picked this because Vertex needs a credit card), OR Claude via `@anthropic-ai/sdk` (`anthropic.api_key`) / `@anthropic-ai/vertex-sdk` (`vertex.project_id`/`region`/`credentials_path`). All three externalized (require() from node_modules — verify `grep require\("@google/genai out/main/index.js`). Model from `coach.model` (default `gemini-2.5-flash`; SettingsPage shows Gemini models when provider=gemini, Claude models otherwise). **Two provider adapters in `gachaCoach.ts`**, both a manual streaming agentic loop (cap 8 iters) sharing tools/context/persistence: `runGeminiTurn` (`models.generateContentStream`, `functionCalls` → run tools → `functionResponse` parts, `parametersJsonSchema` = same JSON schemas) and `runAnthropicTurn` (`messages.stream`, `stop_reason==='tool_use'`, `buildModelParams` opus/sonnet→`thinking:{type:'adaptive'}`/haiku→`{}`, NEVER temperature/top_p). Both stream text deltas into `status.partialText`, phase `tool:<name>` on tool starts. **History is provider-neutral plain text** (`textOf` of stored `api_blocks`) so switching providers mid-thread is safe and both adapters build their own wire format — assistant rows persist `[{type:'text',text}]`, no verbatim block replay. Pure logic (tools, `buildContextBlock`, `buildModelParams`, `npLevelToDupes`, `geminiToolDeclarations`/`toolDefinitions`) lives in `coachTools.ts` so tests skip all SDKs. **17 tools** execute sync against gachaRepo/coachRepo (get_roster/add_unit/update_unit, currencies, banners, goals+recurring tasks, save_note, get_news — NO deletes); FGO vocabulary: np_level 1-5 → `dupes=np-1`, add_unit dedupes by (kind, lower(name)). Every mutator pushes an action chip. **Status+poll like musicDownload** (module-level `status`, `gacha:coachStatus` polled 350ms, `lastSettled` guard by turnId → invalidate broad `qk.gacha.all`); cancel via `AbortController` (Gemini `config.abortSignal`, Anthropic `stream` abort). **Two rows/turn persisted**; user row = text + `[screenshot attached]` markers — **images NEVER replayed**, only sent (as base64) the turn they arrive. Context: fresh `<context>` block (roster/currencies/banners/goals w/ `[OVERDUE]`/`[DUE TODAY]`/notes/doc summaries/news) prepended to the CURRENT user turn only. Vision: `nativeImage` resize long-edge≤1568 → JPEG base64 (Gemini `inlineData`, Anthropic image block). Reminders render from DB with ZERO LLM calls; `dueCounts` drives badges on the Coach pill + FGO hub card. Assistant markdown via `@shared/markdown.ts` pure parser → `components/Markdown.tsx` (NEVER dangerouslySetInnerHTML). All 5 coach tables (`gacha_chat_thread/message`, `gacha_goal`, `gacha_coach_note/doc`) + `gemini.api_key`/`anthropic.api_key`/`vertex.*` settings are personal → wiped in sanitizeSql.cjs. `abortActiveCoachTurn()` in before-quit.

**FGO catalog + Chaldea import (2026-07-21)** — the second FGO detail phase, gated by `GachaGameCfg.catalog` (`{ source:'atlas', backup?:'chaldea' }`, fgo only; both keys FROZEN — `source` is written to `gacha_unit.external_source`). **Atlas Academy** (`api.atlasacademy.io`, NO key) is the FGO catalog source: `src/main/atlas.ts:importCatalog` fetches `/export/NA/basic_servant.json` (421) + `basic_equip.json` (2118), stages them (servant kind + `prettifyFgoClass(className)` element, craftEssence kind), ONE `downloadImages` batch of every `face` (activity pill), then `gachaRepo.upsertCatalogUnits` seeds them all as **owned=0** rows. Two new repo fns own their transactions (replaceNews precedent): `upsertCatalogUnits` (INSERT owned=0 … ON CONFLICT(game,kind,external_source,external_id) DO UPDATE **canonical only** — personal fields never in DO UPDATE, `image_path=COALESCE(excluded,…)` keeps art when a face download fails; a NULL-external manual row with a matching name is ADOPTED not duplicated) and `applyOwnership` (Chaldea backup → flip owned=1 + dupes/level + shallow-merge into `data`; **throws if catalog empty**; non-destructive — rows absent from the backup are untouched). Re-import is authoritative for name/rarity/element/image, preserves owned/favorite/level/dupes/notes/data. **Chaldea import** (`src/main/chaldea.ts`): `parseChaldeaBackup` is the trust boundary (userdata.json `{users,curUserKey}`; servant `cur.favorite`→owned, `_npLv`1-5→dupes, dataMerge ascension/skills/appendSkills/grail/fou/bond with per-field validators; CE `status===2`→owned, `limitCount`→dupes, `lv`→level; bad file throws, bad entry skipped) → `importBackup` picks the file **in main** (JSON never crosses IPC) via generalized `files.pickTextFile({extensions,maxBytes})` (default txt/md 2MB unchanged; Chaldea passes json/64MB). IPC: `gacha:importCatalog` (withActivity) + `gacha:importChaldea`. Renderer: a **Catalog tab** (GachaGamePage, only when `cfg.catalog`) mirrors RosterTab but lists ALL rows — unowned cards dim (`opacity-50`) + open `GachaUnitDialog` in **claim mode** (`unit && !unit.owned` → "Add to roster", save adds `owned:true`); RosterTab itself adds `ownedOnly:true` to its filter for catalog games (non-catalog games keep byte-identical filter/cache keys). GachaUnitPage: catalog rows (externalSource) show **Remove from roster** (`owned:false`) not Delete (re-import would resurrect a deleted row). **Coach is catalog-aware** (coachTools.ts): `gatherSnapshot`/`get_roster` force `ownedOnly:true` (else 2.5k unowned rows swamp the context caps); `add_unit` on an existing row flips owned=1 + never clobbers a catalog row's canonical rarity/element (Atlas authoritative); `findUnit`/`update_unit` on an unowned catalog row returns an "use add_unit" error. NO schema change (columns + `idx_gacha_unit_external` predate this); NO sanitizeSql change (gacha_unit already wiped — catalog rows go with it, fine). Tests: atlasImport (convergence on pre-seeded rows, manual adoption, COALESCE image, atomicity, prettify), chaldeaImport (parse + apply, non-destructive, catalog-empty throw), gachaCoach (owned-only, catalog-aware add_unit). Future: catalog importers for the other games (add `catalog` to their cfg + a source module); material/ascension planner off `gacha_unit.data`.

**Seasonal anime (2026-07-10)** — `/anime/seasonal` (`SeasonalAnimePage`) shows the library's anime as four stacked Winter→Fall shelves per selected year; entry points are `MediaConfig.hasSeasonal` (list-header button), an `ANIME.children` sidebar link, and a CommandPalette nav item (route sits above `/anime/:id` in App.tsx). Season data: the AniList anime importer persists canonical `season`/`seasonYear` into the `metadata` JSON (`mergeMetadata` accepts positive numbers AND non-empty strings — mirrored in bulk-import.cjs); `@shared/season.ts:seasonForItem` (tests/season.test.ts) prefers validated metadata, falls back to release-date quarters (Jan–Mar=winter …), else the page's Unknown bucket. Known skew: year-only AniList dates are padded to `-01-01` → Winter, and Dec premieres belong to next-year WINTER — both self-correct on re-import. The page reuses `qk.media.home('anime')`, so its queryFn filter must stay byte-identical to HomePage's (`{ mediaType: 'anime' }`).

**Songs / theme library (2026-07-25)** — `/anime/songs` (`ThemeSongsPage`, an `ANIME.children` sidebar link + CommandPalette item; route sits above `/anime/:id`) replaces the sidebar's old "Shuffle Themes" button (gone, with `player.tsx:quizSongToTrack` — the page maps its own tracks). Every imported OP/ED as a playable list, where **the anime half of the filter IS `MediaListFilter`**: `repos/themeRepo.ts:list({media, search, songType, favoriteOnly, playableOnly})` calls mediaRepo's now-exported `buildWhere`/`buildOrder` (alias the media table `m`), so statuses/tags/ranges/favorite/season mean exactly what they mean on the anime list page — the page literally reuses `MediaFilterPanel`, the sort menu and `qk.mediaCounts.facets('anime')`. `mediaType` is forced to `'anime'` in the repo. Song-level extras: OP/ED, hearts, and a search that also matches song title/slug/artist (wider than the media filter's title-only search). Ordering groups songs under their anime (`COALESCE(sort_order,1000), ts.id`) EXCEPT `random`, where `buildOrder`'s new `randomIdExpr` arg hashes `ts.id` so a shuffle deals songs, not whole shows; every play button queues the WHOLE filtered set (`theme-<id>` id namespace, unchanged). **New personal column `theme_song.favorite`** (init.sql + schema.ts + `ensureColumn`, wiped in sanitizeSql.cjs, also on the detail page's `ThemeRow` heart): the AnimeThemes import is a clean replace, so `themes.ts` snapshots hearted `external_id`s before the DELETE and restores them on insert — mirrored in bulk-import.cjs. Tests: themeRepo.test.ts (shared-filter reuse, song filters, seeded song shuffle), themeImport.test.ts (favorites survive a refresh).

**Phone sync / Android companion — REMOVED (2026-08-01).** A LAN sync server (`sync.ts`, `syncOps.ts`, `SyncOp`/`SYNC_PROTOCOL_VERSION`, the `sync:*` IPC group, the Settings "Phone sync" card, `sync_batch`) shipped 2026-07-16 as Phase 0 of a Capacitor companion app. **The user scrapped the idea — do not rebuild it, and do not treat it as pending work.** NaviHUB is desktop-only. Two deliberate leftovers: the `sync_batch` table and the `sync.*` settings keys still exist in the user's live DB (removing them from init.sql doesn't drop them) so sanitizeSql.cjs keeps wiping both — `sanitizeDb` skips tables a DB doesn't have, so it's a no-op on fresh installs. Git history has the code if it's ever wanted back.

**Torrent search (2026-07-21)** — search + orchestration only, NO torrent code: Jackett (user-run, aggregate JSON endpoint `/api/v2.0/indexers/all/results?apikey&Query&Category[]`) supplies results, qBittorrent's WebUI API receives them. `src/main/jackett.ts` (pictures.ts pattern: pure `jackettSearchUrl`/`parseJackettResults` + throwing `searchTorrents`; interactive calls use `rateLimitWaits:0, timeoutMs:20s, retries=1` to fail fast) and `src/main/qbittorrent.ts` (module-level SID session; form-encoded login → `Cookie: SID=…` + `Referer` on `/torrents/add`; creds OPTIONAL = bypass-auth-for-localhost mode skips login; 403 → re-login ONCE, never loop — qBittorrent IP-bans repeated failures; 200 `'Ok.'`/empty = success, `'Fails.'` = duplicate/invalid). Magnet preferred, Jackett-proxied `Link` fallback (`pickAddUrl`). Entry points: unconditional "Find torrents" button on every media detail page (`TorrentSearchDialog`, query prefilled+auto-run from title, categories from `@shared/torrents.ts:torznabCategoriesFor` — anime 5070, manga 7030+5070, game/VN 4050, movie 2000, tv 5000, with an All-categories escape hatch) and the `/torrents` sidebar page (footer, beside Settings; `usePersistedState`). Shared `TorrentResultsTable` sorts client-side (seeders desc default, nulls last), per-row Add with ImageBrowseDialog's pending-map pattern, Details via `app:openExternal`. Settings → Integrations: two Save-&-test cards; Jackett probe = Torznab `t=caps` (instant, hits no indexer — check body for `<error`), qBittorrent probe = `/app/version`; both probes never throw. All 5 settings keys (`jackett.url/api_key`, `qbittorrent.url/username/password`) are wiped in sanitizeSql.cjs. `qk.torrents` is never invalidated (results are ephemeral); dialog/page queries use `retry: false` (main already retried). Tests: torrents/jackett/qbittorrent.test.ts (gachaNews recipe — mocked http, real settings SQL). **Start Jackett button** (`StartJackettButton`, on the Torrents page header, both search-error blocks, and the Settings card): `torrents:ensureJackett` → `ensureJackettRunning` probes the base URL (no API key needed — any HTTP answer = up), and ONLY if down + loopback (`isLocalHost`) runs `jackett.start_cmd` (default `systemctl start --no-ask-password jackett.service`) via `execFile` with **no shell** (argv split by `parseStartCommand`), then polls 45×1s — `--no-ask-password` is load-bearing (a spawned process has no askpass, so a polkit challenge would hang forever). Verified live against the real unit: stop → button → running again in ~13s, which is why the poll budget is generous. `jackett.start_cmd` is machine-specific → wiped in sanitizeSql.cjs. **`TorrentSearchDialog` takes explicit `categories`** (not a MediaType), so non-media callers work: MusicArtistPage's "Find torrents" passes `AUDIO_CATEGORIES` + `discographyQuery(name)` ("<Artist> discography") via `MusicEntityHeader`'s `extraActions` slot.

**Progressive search + filters (2026-07-21)** — the aggregate endpoint blocks until the SLOWEST indexer answers (measured on the user's 59-indexer instance: 16.7s for a cold query, and a 29.6s case where 56/59 finished in 6.6s but 3 stragglers held the rest), so search is now a **fan-out job**, qBittorrent-style. `jackett.ts` owns a module-level `SearchJob` (musicDownload status pattern — no push channel): `startSearch` (throws synchronously if unconfigured, else returns a job id immediately) enumerates indexers via **`t=indexers&configured=true`** — the ONLY indexer list reachable with just the API key, since `/api/v2.0/indexers` 302s to Jackett's login page even with no admin password (verified live) — then runs a 16-worker pool of per-indexer `searchTorrents(query, cats, indexerId)` calls, pushing rows in as each returns. Empty/failed indexer list ⇒ falls back to ONE aggregate request (`all`), so locked-down Jacketts still work, just not progressively. Workers guard on the job id, so starting a new search or cancelling makes stale ones drop their rows; `cancelSearch` only sets `running=false` and **keeps** what was found (qBittorrent's Stop). `searchStatus(offset)` returns only rows past what the poller holds — a 1359-row search polled every 400ms would otherwise re-clone everything. Renderer: `useTorrentSearch` (lib/) starts + polls via `refetchInterval` while running, accumulating by offset and cancelling on unmount; `TorrentResultsPanel` (shared by page + dialog) shows `N/M indexers · X results`, Stop, and `TorrentFilterBar`. **Filters are pure + tested in `@shared/torrents.ts`** (`applyTorrentFilters`/`EMPTY_TORRENT_FILTER`/`sizeToBytes`): title contains-all-words, exclude-words, min seeders, size min/max (MiB/GiB), tracker multi-select chips built from the results present — a null size/seeder count means UNKNOWN and is kept unless a bound excludes it. Only the standalone page skips auto-run (a fan-out per navigation is too much tracker traffic); the prefilled dialog auto-runs. `TorrentResultsTable` renders through `useIncrementalList` (1000+ rows).

**Search precision (2026-07-21)** — raw indexer results are noisy: substring bleed (searching `akagi` returned `Nige Jouzu no Wak`**`agi`**`mi`) + irrelevant indexers (E-Hentai, audiobook-only). Two fixes, both in `@shared/torrents.ts` (pure, tested) — measured live: `akagi` cat 5070 went 59→30 indexers and 198→44 results. (1) **Word-boundary relevance** (`queryTokens`/`titleMatchesQuery`/`relevanceFilter`): every query token (≥2 chars) must appear in the title as a whole word via Unicode `(?<![\p{L}\p{N}])…(?![\p{L}\p{N}])` lookarounds (CJK-safe); a token-less query is a no-op (never blanks a page). Applied renderer-side in `TorrentResultsPanel` as a **default-on toggle** ("Relevant only · N hidden") BEFORE `applyTorrentFilters` — instant Show-all, never re-queries; `useTorrentSearch` exposes the active `query` to drive it. (2) **Category-scoped fan-out** (`indexersForCategories`, called in `jackett.ts:runSearch`): `parseIndexerList` now also parses each indexer's advertised standard categories (`<category>/<subcat>` ids < 10000); an anime search (`[5070]`) only hits indexers sharing a thousands-bucket (5xxx), skipping audiobook/movie-only ones. Empty categories ("All categories") ⇒ no scoping; indexers advertising no standard cats are kept (unknown coverage); zero matches ⇒ fall back to all. NOTE: general trackers that *claim* anime support still get queried, so the word filter (not scoping) is what removes their junk — scoping is the speed/scope win. No IPC/type contract change (relevance renderer-only, scoping internal, `IndexerInfo` internal to jackett.ts).

**Tournament quiz mode (2026-07-16)** — `/quiz/tournament` (`TournamentPage`, card on QuizLandingPage): uwufufu-style single-elimination world-cup bracket — two contenders side by side, click (or 1/←, 2/→) picks the winner until a champion. Pool sources: music tracks (all/liked/playlist/artist/album), anime theme songs (delegates to `quizRepo.songPool`), characters (all, subtitled by first linked media title, or one title's roster), media by type+status, people by credit role, and any custom list. **Pure engine `src/shared/bracket.ts`** (index-based — the page shuffles entries and the bracket only speaks pool indices; byes pre-resolved at creation so a bracket is always exactly n−1 picks; `pickWinner` is immutable, so undo is a page-level snapshot stack, NOT an engine feature). Pool via ONE channel `quiz:tournamentPool` → `repos/tournamentRepo.ts`, a normalizing facade over existing repos (only characters have their own SQL) returning the WHOLE set — the renderer shuffles + caps (8/16/32/64/all), which lets "Run it back" resample without refetching. One-sitting, not resumable; the finished bracket logs a `quiz_session` with kind `'tournament'` (score=total=poolSize; settings JSON holds sourceLabel/champion/runnerUp) — the setup screen renders its own history block, NOT `QuizRecord` (accuracy framing is meaningless at a constant 100%). Audio auditions play through the shared player under the **`tourney-<key>` id namespace with a guarded stop (`player.track?.id.startsWith('tourney-')`)** so play-count logging/highlighting never react and an image-only bracket can't kill background music; entries pass raw audioPath/audioUrl (the player resolves lazily). Tests: bracket.test.ts (exhaustive n=2..17 simulation), tournamentRepo.test.ts.

**Library filtering & sorting (2026-07-19)** — every media type's `MediaListPage` gets one filter model, all of it inside `MediaListFilter` (shared/types.ts) so the whole filter object stays the query key. Repo side (`mediaRepo.list`): `buildWhere()` assembles clauses for multi-status (`statuses`, superseding the legacy single `status`), multi-tag with `tagMode:'any'|'all'` (legacy `tagId` still honored), favorite, `unrated` (score IS NULL — wins over any score range), and inclusive numeric ranges for personal score, community score, year and `total_units`. Three derived SQL expressions are shared by filters/sorts/facets and must move together: `COMMUNITY_SQL` COALESCEs every importer's rating key into ONE 0-100 scale (averageScore / metacritic / vndbRating / imdbRating×10 — **any new importer rating key belongs here**), `YEAR_SQL` (release year, falling back to metadata `seasonYear`), and `SEASON_SQL` (metadata season first, then month quarters — mirrors `seasonForItem` in @shared/season.ts, keep in step). Sort keys come from the `SORT_SQL` whitelist (never interpolate filter input); `random` is a SEEDED multiplicative hash of `m.id` — the multiplier carries the seed (an additive seed only rotates the same order) and is folded in JS so `id * mult` can't overflow int64, which makes a shuffle stable across refetch/scroll-batching while the page's Shuffle button re-seeds. `mediaRepo.facets(mediaType)` (`media:facets` → `qk.mediaCounts.facets`) feeds the sliders real library bounds and is the page's true title count — `statusCounts` drops NULL statuses and undercounts. Renderer: `MediaFilterPanel.tsx` owns `MediaFilters` + `EMPTY_FILTERS`/`activeCount`/`toListFilter`, range convention **`null` = unconstrained** (a slider spanning its full bounds collapses back to null, which is what keeps untouched filters out of the query key); `RangeSlider.tsx` is the app's dual-thumb slider (two transparent native ranges over a div track, thumbs styled in the styles.css WIRED CHROME block). Status pills are multi-select; active narrowings render as removable chips with Clear all. HomePage/SeasonalAnimePage still pass `{ mediaType }` only, so their shared `qk.media.home` cache entry is untouched — don't add defaulted fields at those call sites.

**Checklist (2026-07-25, reworked 2026-07-27)** — `/checklist` (`ChecklistPage`, sidebar link directly under Home, plus a "Routine" card in the HomePage right rail): a daily + weekly recurring board. The CATALOG of possible items is CODE (`src/shared/checklist.ts`, `CHECKLIST_DEFS` — the `GACHA_GAMES` idiom); in-app the user can only add/remove items, retarget them and reorder them, so new KINDS are a code change. `key` strings are FROZEN (stored in `checklist_task.task_key`/`checklist_log.task_key`). Three item kinds: **mediaLog** (the checklist performs the tracking write — anime/manga/TV episodes, films), **detected** (completion is READ from tables the app already writes — `jp_review_log` distinct `card_id`, `jp_lesson.learned_at`, `quiz_session.played_at`; clicking the row navigates to where the activity happens) and **manual** (plain tick; one per gacha game, generated from `GACHA_GAMES`). **Every detected item also takes hand-made credits** (`checklist:credit`, un-clamped, one log row each, individually undoable): `progress = detected + credits`, and the streak pass mirrors that sum — v1 had no override and the user hit exactly the dead end it creates. Manga was detected from `manga_chapter.read_at` and is now mediaLog for the same reason: those rows only exist for locally-scanned series read in the in-app reader, so any other way of reading manga could never tick it (progress may then run ahead of the scanned chapter list — harmless, `manga.ts:syncMediaProgress` is monotonic).

**Progress logging is app-wide, not a checklist silo.** `@shared/mediaProgress.ts` is the single pure definition of "I watched/read another one": `advanceProgress(state, statuses, unitProgress)` does status promotion (null/planned → in-progress, last unit → completed) and, on an already-finished title, **wraps into a fresh pass** — `progress` back to 1, `rewatch_count = max(count,1)+1` (that column is *times consumed*, which is how `mediaRepo.timeStats` reads it), flagged by `startedRewatch` so callers announce it instead of silently resetting. It also owns `STATUS_FALLBACKS`/`parseStatuses` (main can't import mediaConfig.ts) and `UNIT_PROGRESS_TYPES` (mirrors `MediaConfig.unitProgress` — keep in step). `checklistRepo.logProgress(mediaId, today, task?)` is the ONE write path: with a task (checklist Log button) or without (every media detail page's log button, `media:logProgress`, labelled from `MediaConfig.logUnitLabel`), in which case it credits whatever mediaLog item covers that type, daily before weekly, or none. `payload.prior` snapshots progress/status/rewatchCount so undo restores a rewatch too.

Two tables (`checklist_task` = the enabled board incl. a nullable `target` override, `checklist_log` = events) with NO FK between them and none on `media_id` — removing + re-adding an item keeps its history, and reads LEFT JOIN + fall back to the cached title (jp_card.source_media_id precedent). `target` shipped one build later → needs its `ensureColumn` as well as the init.sql column. **Weeks run SATURDAY→FRIDAY** (`WEEK_START = 6`); `period_key` is the local day for daily rows and the week's Saturday for weekly. **Every date is computed in MAIN as a LOCAL day** and passed into the repo (`status/logProgress/tick/credit/untick` all take `today` — coachRepo's testability pattern); `ipc.ts`'s `todayLocal()` sits at the top of `registerIpc`, shared by gacha goals + checklist. The renderer only FORMATS main's date strings — never `new Date()` for a day, which is the UTC-vs-local bug the gacha coach's `CoachRail` still has. ONE read channel `checklist:status` returns board + streak + 52-week heatmap; a day is "complete" when every daily item that already existed then (`date(created_at,'localtime') <= day`) hit its (possibly overridden) target, fed to the shared `musicRepo.computeStreaks`. The board **seeds itself on first launch** (`seedChecklist` in connection.ts, `CHECKLIST_SEED`, gated by the `checklist.seeded` setting — wiped on export alongside both tables, or a recipient's board never seeds). Page/Home queries use `staleTime: 0` (+ a 60s interval on the page) so returning from `/japanese/review` re-detects and an idle page rolls over at midnight; log/undo invalidate `qk.checklist.all` + `qk.media.all`. UI is the StatsPage hero treatment (minus its glyph) over tight rows with inline progress bars; edit mode swaps in drag handles (`SortableList`/`useOptimisticReorder` — memoise the mapped rows, its re-sync effect keys on array identity), target inputs and remove buttons. `ChecklistAddDialog`'s cadence is CONTROLLED by the page (`adding` IS the cadence) — v1's local `useState(cadence)` copy is what made daily/weekly appear swapped. Picker reuses `api.media.list({mediaType, search})` (GachaGameImageDialog pattern — NOT `UniversalPicker`, whose global search caps at 20 rows across all types), floats in-progress/planned titles to the top and marks finished ones "Again". Tests: mediaProgress.test.ts (pure rules incl. rewatch arithmetic), checklist.test.ts (`process.env.TZ='UTC'`, anchored on Sat 2026-07-25).

**English dictionary (2026-07-29)** — `/english` (Learn section, one page): English→English lookup via **dictionaryapi.dev** (no key) in `src/main/english.ts` — pure `parseEnglishEntries` (fixture-tested) + `lookup` through `fetchWithRetry` (`timeoutMs: 15s, rateLimitWaits: 0`; **404 = unknown word = `[]`, anything else throws** so the QueryCache toast fires — the deliberate opposite of jisho.ts's swallow-everything). Saving a definition writes `en_word` (word, phonetic, pos, meaning, example) via `englishRepo.saveWord` — **idempotent on exact (word, meaning)**, returns the existing id — and the saved list renders on the same page from ONE unfiltered `qk.english.words('')` query (saved-state chips + client-side filter both read it; the repo's LIKE search exists but the renderer doesn't use it). Deliberately NOT part of the jp SRS ("save it for me, just that"). `en_word` is personal → wiped in sanitizeSql.cjs.

**Offline English dictionary (2026-08-01)** — the online API is now the FALLBACK: `english.ts:lookup` tries `lookupOffline` first when a bank is installed (mirroring `dict/lookup.ts:lookupWord`), and `EnDictEntry.source` (`'offline' | 'online'`) badges the difference in the UI. The data lives in the EXISTING `dictionaries.db` (dict/init.sql + `sweepOrphans` + the version-bump DROP list, all extended) as `en_dict` registry + `en_lemma` / `en_synset` / `en_exc` / `en_pron`. `src/main/dict/wordnet.ts` imports **WordNet 3.0** (one 10.7 MB zip from nltk_data — it is the only mirror that ships the `*.exc` morphology files; the npm `wordnet-db` package does NOT) plus **CMUdict** for IPA, both pinned to a commit sha (KanjiVG precedent; nltk's gh-pages and cmudict's master both move). It reuses importer.ts wholesale — `runImport` (the single global gate), `downloadToTemp`, `openZipReader`, `setImportPhase`/`setImportProgress` — so progress renders through the same polled `dict:importStatus` (two new phases `english`/`pronunciations`) and the two dictionary cards share one busy state. Same stage-then-swap discipline as sentences.ts: everything under a fresh `bank_id`, registry row LAST, catch deletes the staged rows. **`importWordNetText(files, cmuText)` is the seam** — tests drive the real SQL from real-format fixture slices with no network or zip. Parsing is hand-rolled and fixture-tested: WordNet's index (offsets are the sense order — that's why the index files are kept rather than deriving lemmas from the data files), data (HEX word counts, `(a)`/`(p)` markers stripped, `s` satellites stored as `a`), the exception lists, and ARPABET→IPA. Lookup candidates = the word + `en_exc` hits (ran→run) + `morphyCandidates` (WordNet's suffix rules, over-generating on purpose like deinflect.ts). Per-definition synonyms are the rest of that synset; meaning-level synonyms are deliberately EMPTY offline (unioning them across senses claims a mouse is both a rodent and a black eye). Verified against the real full data: 155k lemmas / 118k synsets / 126k prons, ~10s import, 30 MB, 1-8 ms lookups. Settings → **Dictionaries** (the `japanese` tab, relabelled; key unchanged) holds both cards.

**Japanese wave 2 (2026-08-01)** — five new data packs, eight new drills, and a TheMoeWay-shaped roadmap. **Packs** (all in `dictionaries.db`, additive `CREATE TABLE IF NOT EXISTS` — NO DICT_SCHEMA_VERSION bump; every new child table is in `sweepOrphans` AND the version-bump DROP list, kept honest by tests/dictSchemaSync.test.ts which diffs init.sql against both lists): **Kanjium pitch** (`dict/kanjium.ts`, sha-pinned raw TSV → the EXISTING `pitch` table under a `dict` registry row titled `Kanjium Pitch Accents` — `loadPitches` has no dict filter, so the dictionary page's PitchAccent rendering lit up with zero lookup changes; `listDictionaries` gained a `pitch_count` subselect + `DictInfo.pitchCount`); **KRADFILE components** (`dict/krad.ts`, sha-pinned krad-unicode JSON → `krad_set`/`krad`/`krad_part` (the radkfile inversion, computed at import so search-by-parts is one GROUP BY/HAVING)/`krad_component`; `KanjiInfo.components` rides every kanji lookup); **grammar N5-N1** (`dict/grammar.ts`, five sha-pinned hanabira.org JSONs → `grammar_bank`/`grammar_point`; **cloze fields pre-computed at import** via `@shared/cloze.ts:grammarPointCandidates`/`clozeGrammarExample` so the drill pool filter is `clozeJp != null`); **JMnedict names** (a PRESETS entry through the normal Yomitan pipeline with new `importFromReader` opts `{glossFts:false, defaultPriority:-10}` — skipping gloss_fts is THE English-search pollution fix (searchEnglish ranks by FTS score only) and priority −10 sinks name groups below words; `DictEntry.isName` is computed in lookup.ts from the dict title (a registry column would force a version bump that wipes installed dicts), and the same commit fixed the latent defs-order bug: group defs now sort by dict PRIORITY, not id, so JMnedict never outranks JMdict inside a merged group like 東京; `dict/names.ts:nameSample` aggregates ALL readings per expression); **audio** — first audio in the section, under the new `jpaudio/` navimg prefix (`files.ts:jpAudioDir()`, always userData): **minimal pairs** (`dict/minimalPairs.ts`, sha-pinned codeload zip of Kuuuube/minimal-pairs; per-pair JSON with BASE64 audio decoded to `jpaudio/pairs/`, container sniffed by magic bytes — the pack ships ADTS AAC, not mp3) and **Tatoeba sentence audio** (`dict/tatoebaAudio.ts` + runtime dep **`seek-bzip`** (pure-JS bz2; typed in src/main/env.d.ts; verify `grep 'import("seek-bzip")' out/main/index.js`): two bz2 exports → plan → ~6k throttled per-clip fetches (300ms, single worker, `rateLimitWaits:0`, abort after 20 consecutive failures), **resumable** — clips are keyed `jpaudio/tatoeba/<audio_id>.mp3` and existsSync-skipped on re-run; rows join the sentence bank BY EXACT JP TEXT so bank re-imports never orphan them; empty license column = clip skipped, per-clip license/attribution stored and rendered; `SentenceExample.audioPath` joins in via a subselect in `sentences.ts:querySentences`; the Settings poll now self-gates on `status.running` (not just local busy) so the ~20-min run survives navigation, and BOTH dictionary cards disable while ANY import runs). **Drills** (QuizKind += `pitch pairs components grammar names numbers dictation shiritori`; every pack-gated setup degrades to EmptyState→Settings; audio drills use a page-local `new Audio(mediaUrl(...))` — NEVER the global player queue — plus a one-shot courtesy `player.toggle()` if music is playing): `/japanese/pitch` (Patterns = contour MC over `japanese:pitchQuizPool`, cards×Kanjium with freq top-up; Minimal pairs tab = kotu mechanic, 1/2 answer, R replay, endless log-on-Stop), `/japanese/listen` (dictation: graded on kuromoji READING keys via `@shared/dictation.ts`, raw-text fallback when tokenize returns []; per-char LCS diff on reveal), `/japanese/kanji` (by-parts browser) + `/japanese/kanji/quiz` (build-a-kanji over `japanese:componentQuizPool` — kanji-KIND lessons, not the title heuristic; decoys stroke-biased in main), `/japanese/grammar` (library: level pills + client-side search over ONE cached `dict:grammarList`; `?q=` deep link; grammar lessons link in via `grammarCandidates(title)`) + `/japanese/grammar/quiz` (cloze MC), `/japanese/shiritori` (validation = `dict:lookup` exact-form; replies = `dict:shiritoriNext`, freq-jittered nouns, JMnedict excluded via `priority >= 0`; logs tournament-style score=total=chain), and JapaneseKanaPage ("Typing Drills") grew **Numbers & counters** (endless per-keystroke over `@shared/numbers.ts` — the euphonic tables ARE the feature, exhaustively tested — checked by `@shared/typing.ts:matchState`, whose invariant is never-'wrong'-while-a-completion-exists) and **Names** tabs, with `?tab=` seeding (also on /japanese/pitch). The generic Drill engine moved VERBATIM to `components/japanese/TypedDrill.tsx` (KanaDrill stays in-page). The practice quiz is now **multi-direction** (`directions: Direction[]` under NEW key `jpQuizDirections`, default `['jp2en']` preserves old behavior; per-question random supported direction; sub-pools per direction, <4-distinct-answer directions silently dropped; kind stays `'japanese'`). Cross-DB pools live in `src/main/jpDrills.ts` (coreDeck pattern); pools are plain awaits at Start, never cached. **Roadmap** gained the Daily-loop strip (`RoadmapDailyLoop` — reviews/lesson/immersion; the immersion tile's query must stay byte-identical `{ mediaType: 'manga' }`, shared qk.media.home cache) and Milestones (`RoadmapMilestones` over `media:jpMilestones` — `mediaRepo.jpMilestones()` resolves completed POSITIONALLY via `@shared/mediaProgress.parseStatuses`; novels = manga-type completed with an `.epub` manga_chapter; `qk.media.jpMilestones` sits under the `['media']` prefix on purpose). No sanitizeSql changes anywhere: dictionaries.db and `jpaudio/` are not exported, quiz_session was already wiped. Pinned shas live as constants in each dict module; JMnedict deliberately uses `releases/latest` like the other presets.

**Programming section (2026-07-29)** — `/programming` (Learn section): interactive courses + CLI cheatsheets + a typed practice drill. **All content is CODE** (`src/shared/programming/`: `types.ts`, `courses.ts` aggregating one file per course — `goCourse` (17 lessons), `regexCourse`, `gitCourse`, `sqlCourse` — and `cheatsheets.ts`), the checklist.ts idiom taken further: unlike Japanese (seeded to DB because the user edits cards), courses are read-only so app updates update content. Course/lesson/sheet `key` strings are FROZEN; the only table is `prog_progress` (`lesson_key` = `'<courseKey>/<lessonKey>'`, personal → sanitizeSql) and `programmingRepo.complete` validates keys against the catalog. **Lesson bodies are Markdown for `components/Markdown.tsx` — author within its parser's limits** (headings 1-3, FLAT lists, bold/italic/inline code/links, ``` fences; NO tables/blockquotes/nesting), and remember bodies are template literals: escape every backtick and `${`. Pages: `ProgrammingHomePage` (course cards + progress bars), `ProgCoursePage`, `ProgLessonPage` (Markdown body + click-to-check questions — self-check only, nothing logged — + Mark-complete toggle; remounts via `key={courseKey/lessonKey}` so prev/next resets question state), `CheatsheetsPage` (tabs; a non-empty search cuts across ALL sheets), `CliPracticePage` — a **KanaDrill port** (per-keystroke check, red-and-stay miss, Enter reveal/skip with 3/13 re-queue splices, endless, Stop logs `quiz_session` kind **`'cli'`**) with two command-specific twists: Space is typeable (not an Enter alias) and comparison goes through `normalizeCmd` (collapse whitespace, uniform ` | ` around pipes). Practice pool = only cheat entries WITH an `answers` array (canonical spelling first, flags-not-filenames convention); entries without one are reference-only. Adding a course = one new file + an entry in `PROG_COURSES`; adding drillable commands = data only. tests/programming.test.ts validates the whole catalog (unique kebab keys, balanced fences through the real `parseMarkdown`, 4-option questions, normalized answers).

**Programming round 2 (2026-08-01)** — content: 6 courses (`goCourse` 17 lessons, `shellCourse` 8, `dockerCourse` 7, `regexCourse` 7, `gitCourse` 7, `sqlCourse` 7) and 13 cheatsheets (added `docker`, `tmux`, `node`, `python`). tmux's key bindings are entered WITHOUT `answers` on purpose — they are keystrokes, not commands, so they read on the sheet but never enter the typing drill. **`ProgrammingQuizPage`** (`/quiz/programming`, new `QuizKind` `'programming'`) is the section's entry in the Quiz hub: the JapaneseQuizPage loop (setup/play/summary, 1-4 + Enter, one `loggedRef`-guarded `endGame` that decides newBest before invalidating) over two renderer-built pools — the courses' own `ProgQuestion`s (options reshuffled, `correct` re-found by identity) and a "which command does this" MCQ whose distractors come from the same sheet first, then all sheets. No IPC and no repo: the catalog is shared code, so the whole quiz is client-side. QuizLandingPage gained cards for it and for the existing CLI drill.

## Adding features: two flavors

**A) New media type** (fits `media_item`): add to the `MediaType` union in shared/types.ts; create a `MediaConfig` in `src/renderer/src/lib/mediaConfig.ts` and append to `MEDIA_CONFIGS`; add the 4 routes in `App.tsx` pointing at the shared `MediaListPage`/`MediaDetailPage`/`MediaFormPage` with `cfg={…}`. `MediaDetailPage` is TABBED (2026-07-28): Overview / `castSectionTitle` / an optional media tab named by `cfg.mediaTabLabel` (Theme Songs / Chapters / Playtime — absent = no tab, movies/TV) / Art; `?tab=` deep-links a tab (the Comprehension page passes `?tab=media`); the action column has ONE `btn-primary` (log progress) and Delete lives in the `ActionMenu`. Sidebar renders automatically from `MEDIA_CONFIGS`. Config flags drive everything: `castLayout`, `hasCrew`, `listTabs`, `hideFromSidebar`, `importSource`, `hasThemes`, `hasLocalReader`, etc. Optional importer: copy `rawg.ts` (smallest complete example — API key from settings, `fetchWithRetry` from `http.ts`, two-phase import).

**B) Standalone section** (Japanese/Music/Lists style): full vertical slice — tables (init.sql + schema.ts), repo, IPC block, api.ts group, preload mirror, pages + routes in App.tsx, hardcoded sidebar NavLink, `qk.<feature>` query keys, tests. `music_*` (2026-07) is the newest complete reference.

## Import conventions

- **Two-phase atomic**: ALL network work first (API calls + `downloadImages` batch prefetch), then ALL DB writes inside ONE `db.transaction()`. Upsert helpers are synchronous and take pre-downloaded image paths.
- Dedup by `(external_source, external_id)`. **Re-import is authoritative**: refreshes canonical fields, prunes removed children, but preserves personal tracking (status/score/progress) via COALESCE.
- **Re-import to apply**: changes to import logic/ordering only affect existing titles after the user re-imports them — always say so.
- `fetchWithRetry` (http.ts) handles 429/5xx **and applies a per-attempt timeout** (default 30s, `timeoutMs` option) so a stalled host can't hang an import + the activity pill. 429 waits are bounded by `MAX_RATE_LIMIT_WAITS` only and must NOT consume the 5xx/network retry budget (tests/http.test.ts). Callers pass `timeoutMs`, never a fixed `signal: AbortSignal.timeout(…)` — a caller signal wins over the per-attempt timeout and one deadline then spans every retry. `downloadImage`/`downloadAudio` route through it; `downloadImages` runs a 5-worker pool (keep per-image `imageProgress` reporting intact). AnimeThemes needs a User-Agent header (403 otherwise); MusicBrainz-style 1 rps sources get their own throttle.
- yt-dlp args must keep the `--` before the user URL (option-injection guard, enforced by tests/musicDownload.test.ts).

## Renderer conventions

- Query keys ONLY from `lib/queryKeys.ts` (`qk`) — invalidation matches by key prefix, so each group's `all` key must remain a prefix of every key in the group.
- Mutations are plain `await api.…` in event handlers (NOT useMutation); errors surface via the global `unhandledrejection` → toast net in main.tsx. Query errors toast via `QueryCache.onError`.
- Page state that should survive Back: `usePersistedState(name, initial)` from `lib/navState.ts` (never plain useState for filters/search/tabs).
- Big lists: `useIncrementalList` (batch 96) + sentinel div. Search boxes: `useDebouncedValue` before driving queries.
- Tailwind component classes in `styles.css`: `.btn`, `.btn-primary`, `.btn-ghost`, `.btn-danger`, `.chip`, `.card`, `.input`, `.label`, `.pill`/`.pill-active` (single-select/nav pills — THE active-pill look), `.chip-toggle`/`.chip-toggle-active` (square multi-select grids: kana rows, dojo forms), `.card-glow` (accent gradient card). NOTE: a class that `@apply`s `bg-accent` must set `color: rgb(var(--base-900))` itself — the global inverse-video selector only matches literal markup classes (`.pill-active` does this). Grid: `grid-cols-[repeat(auto-fill,minmax(150px,1fr))]`. Keyboard focus comes from ONE global `:focus-visible` accent-ring rule in styles.css — don't add per-component focus styles. `text-gray-600` is reserved for decorative/inactive markers; readable secondary copy uses `gray-400`+.
- Shared UI primitives (2026-07-04, extended 2026-07-06) — reuse before writing new ones: `Section` (uppercase-headed section, `subtitle`/`className` props), `StatTile`, `PageStatus` (page-level loading/not-found), `BackButton` (history-back — ALL detail/form sub-pages use it; fixed-parent `← Japanese`-style breadcrumb Links are the deliberate exception), `SortableList`/`SortableRow`/`useOptimisticReorder` (drag-reorder w/ rollback — playlists + lists; NOT the play queue, which is client-only and can hold duplicate ids → plain ▲▼× buttons), `MusicEntityHeader` (album/artist hero), `playTracks()` in `lib/musicTracks.ts` (every music Play/Shuffle button), `BarChart` (single-series divs chart), `CalendarHeatmap` (GitHub-style year grid), `QuizRecord` (personal best + recent rounds block), `MediaCard` (now `components/MediaCard.tsx` — one card app-wide, `showTypeBadge` for cross-type strips, `showProgressBar` for Continue; MediaListPage re-exports it). **UI consolidation (2026-07-28):** `Tabs` (THE tab idiom — underline horizontal, inset-bar vertical, per-tab `to` renders a Link, `actions` right-slot; replaced four competing tab looks), `PageHeader` (title/subtitle/actions; `back="history"` renders BackButton, `back={{to,label}}` renders the quiet breadcrumb — encodes the back convention), `ActionMenu` (text "More" trigger + popover; where Delete and rare actions live — never beside Edit), `DoorCard` (accent-gradient link card, HomePage rail), `EmptyState` (the ONE filled button goes in its `action`), `Pager` (Prev/Next — text labels, no arrows), `StatInline` (compact stat for dense rows — MediaDetail header; StatTile stays for dashboards). **Polish pass (2026-08-01):** `HubCard` (quiet section-hub card — plain card, hover accent border, `to`/`onClick` variants; the hub grids on Japanese/Quiz/Programming homes. DoorCard's glow stays reserved for Home's right rail; GachaHomePage's GameCard is the deliberate exception — it carries the user's per-game hero art), `PillGroup` (`Group`+`Pill` — the labelled single-select pill row on every quiz/tournament setup screen), PageHeader gained `eyebrow` (chip row above the title), `.kbd` (styles.css — THE keyboard-hint chip; quiz options, shortcut lines, Topbar). PageHeader/PageStatus/EmptyState/Section are now adopted app-wide — new pages start from them, never a bare `<h1>`/`Loading…` `<p>`. Loading convention: page-level → `PageStatus`; in-section → `<p className="text-sm text-gray-500">Loading…</p>`. One `btn-primary` per screen is enforced; when an `EmptyState` shows, any header button with the same verb is conditionally hidden (its `action` holds the one filled button). Forms convention: `PageHeader back="history"`, labels are `<label className="label">` (never `<div className="label mb-1">` — double margin), full-width bottom `btn-primary` Save, NO Cancel button (Back/Esc is cancel). **NO emoji and NO decorative glyphs anywhere — user directive 2026-07-12, hardened 2026-08-01 (strict purge: button-label prefixes, title glyphs, watermarks, the palette icon column are all gone; `MediaConfig.icon` is no longer rendered anywhere).** Functional glyphs stay: player transport, reader HUDs, `✕` close (always with `aria-label`), ✓/○ state marks, ★ score/rarity data badges, ♥ like toggles, `←` in back links, ▸/▾/› disclosure.
- Modals use `useDialog(onClose)` from `lib/hooks.ts` (Escape closes, focus in/out) + `role="dialog" aria-modal="true" tabIndex={-1}` on the panel; popovers just add an Escape case to their document-listener effect. Dialog conventions (unified 2026-08-01): overlay `z-50`, backdrop dismiss via `onMouseDown={(e) => e.target === e.currentTarget && onClose()}` on the overlay (never stopPropagation on the panel), close button is `✕` with `aria-label="Close"`. Icon-only glyph buttons carry `aria-label` mirroring their `title`.
- Command palette (`components/CommandPalette.tsx`): Ctrl/Cmd+K anywhere, `/` outside inputs; mounted ONLY in App's shell branch so the chromeless manga reader never sees it. Reuses `qk.search(q)` (shared cache with SearchPage).
- Audio: one global `<audio>` in `lib/player.tsx` (`usePlayer()`); track id namespaces are load-bearing (`theme-<id>`, `quiz-<id>`, `music-<id>`). Music tracks must keep `mediaId: null` (NowPlayingBar links mediaId → anime detail). Repeat modes (`off/all/one`): wrap logic must keep `queue.length > 1` guards so the quiz's single-track queue can't be skipped by OS media keys. Queue edits (`removeFromQueue`/`moveInQueue`) are only exposed on "Next up" rows — the playing index never shifts — and `originalOrderRef` is pruned by object identity (ids can repeat in a queue). Volume/repeat/shuffle persist in localStorage `player.prefs`; `stop()` deliberately does not persist its shuffle reset.
- Music mutations invalidate the broad `qk.music.all` prefix ON PURPOSE — liked/cover state is denormalized into every track-returning query, and invalidation only refetches *mounted* queries, so narrowing buys ~nothing and risks stale UI.
- Quiz pages (song + Japanese): 1-4 answer / Enter advance keydown effects; one `endGame()` funnel per page logs a `quiz_session` row (guarded by a `loggedRef`) and decides "new personal best" BEFORE invalidating `qk.quiz.history(kind)`.
- HomePage resolves per-type statuses positionally from settings via `statusesFrom` (lib/hooks.ts) — first = in-progress, second = completed, last = planned — so renamed statuses keep the Home strips working. Don't reintroduce `defaultStatuses[i]` lookups.
- Stats-page anchors must NOT use `href="#…"` (HashRouter eats them) — scroll imperatively (`scrollIntoView`).
- **Theme (2026-07):** the UI is the Serial Experiments Lain "Wired terminal" — the app's ONLY theme (a switchable normal theme existed briefly on 2026-07-12; the user liked lain and had it removed, incl. `lib/theme.ts`). ALL palette colors + the radius scale are CSS variables in the styles.css `:root` block, surfaced as Tailwind tokens via `rgb(var(--x) / <alpha-value>)` in tailwind.config.js (`base.*`, `accent*`, full `gray` ladder, `white`; `borderRadius` DEFAULT/sm..2xl — `full` deliberately unmapped for pills/avatars/spinner). Never hardcode those hexes in renderer code — change/add vars in `:root`. Content/data colors stay literal on purpose: StatsPage `TYPE_COLORS`, `shared/gacha.ts` game colors, `MusicPlaceholder`, stock `red/green/amber/yellow/sky/purple/black`. The "WIRED CHROME" section at the bottom of styles.css holds: the mono-chrome selector list (headings/buttons/inputs/aside/header/.uppercase → `--font-mono`; body text stays Inter), the CRT overlay (`body::before/::after` z 80/81, above the z-50 UI, disabled on reader routes via the `data-reader` attribute App.tsx stamps), brand glitch (`.lain-crt`/`.brand-glitch` on the Sidebar brand), glows, and the inverse-video override — white-on-phosphor-green is unreadable (1.7:1), so `.btn-primary, .bg-accent.text-white { color: base-900 }`; keep writing `bg-accent text-white` in markup. Any new animation must sit inside `@media (prefers-reduced-motion: no-preference)`. Boot splash `BootSequence` (mounted in main.tsx) runs once per app launch via a module-scope sessionStorage guard (`ui.booted`) — module scope is what makes it StrictMode-safe; don't move it into an effect. Chrome fonts are bundled `@fontsource` packages: IBM Plex Mono (UI) + VT323 (brand/boot only — single weight, no `…` glyph, no JP) — self-origin because CSP has no remote `font-src`. BrowserWindow `backgroundColor` mirrors `--base-900` (`#0a0f0b`) — keep them in sync. **App icon** (`assets/icon.png`, 512², also the user's .desktop shortcut + electron-builder win/linux icon) is a phosphor-duotone Lain portrait; the same 512² art is `src/renderer/src/assets/lain.png`, reused by the sidebar brand avatar AND the HomePage hero mark (the old inline hub-SVG Logo is gone; renderer asset imports need `src/renderer/src/env.d.ts` `/// <reference types="vite/client" />`). The user's desktop launchers use the themed icon name `navihub` (`~/.local/share/icons/hicolor/512x512/apps/navihub.png`) — after changing `assets/icon.png`, re-copy it there too. Sidebar idiom: TEXT-ONLY nav links (no glyphs), `SectionLabel` ("// LABEL ───" terminal-comment headers), active link = `bg-accent/10 text-accent` + inset 2px phosphor bar (`shadow-[inset_2px_0_0_0_rgb(var(--accent))]`, no layout shift), decorative `wired-dot` "Connected to the Wired" footer. `NavGroup` is the one disclosure shell (media types via `MediaSection`, plus Music [Liked/Listening stats], Gacha [per-game], Japanese [Roadmap/Review/Dictionary/Drills], Programming [Cheatsheets/CLI practice]); labels are LIBRARY/PLAY/LEARN only (CURATE dissolved 2026-08-01) — never a label wrapping a single bare link. Structure: top cluster Home/Checklist/Stats, LIBRARY = media + Music + Lists + Tags, PLAY = Quiz + Gacha, LEARN = Japanese/English/Programming, footer = Torrents + Settings. The catch-all route redirects to `/` (Home).

## Tests

`tests/*.test.ts`, run with `npm run test`. Pattern: `createTestDb()` (tests/helpers.ts) builds an in-memory DB from the REAL init.sql; each file does `vi.mock('../src/main/db/connection', () => ({ getSqlite: () => db }))` so repos run their actual SQL. Modules touching electron/fs mock `electron` and `../src/main/files` (see manga.test.ts, music.test.ts). Importer tests mock `http`/`files` with URL-keyed fixtures (rawgImport.test.ts). Design main-process logic with pure exported functions + injectable IO (e.g. the scanner's `TagReader`, yt-dlp's arg-builder) so tests never need real binaries/files. Test the real query, not a hand-written stand-in.

## Misc gotchas

- Right-click cut/copy/paste is a native menu built in `createWindow` (main/index.ts `context-menu` listener) — renderer has no context-menu code.

- Destructive sync guards: `music.startScan` refuses a zero-file walk when `music_track` has rows (unmounted drive whose mountpoint still exists would otherwise wipe playlists/likes/play history — tests/music.test.ts regression); `manga.rescan` has the equivalent guard. Any new scanner that prunes by "not seen" needs the same check.
- `music-metadata` is ESM-only while the main bundle is CJS — it's loaded via lazy `await import()` in music.ts; don't convert to a top-level import.
- yt-dlp/ffmpeg are user-installed external binaries (settings `ytdlp.path`), never bundled.
- `scripts/bulk-import.cjs` duplicates the import pipeline (known debt) — changes to importers may need mirroring there.
- No ESLint config exists; eslint-disable comments are inert.
