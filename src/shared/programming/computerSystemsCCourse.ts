import type { ProgCourseDef } from './types'
import { masteryLesson as lesson, masteryQuestions as qs } from './masteryCourse'

export const COMPUTER_SYSTEMS_C_COURSE: ProgCourseDef = {
  key: 'computer-systems-c',
  title: 'Computer systems and C: beginner to master',
  description:
    'A self-contained path from binary representation and C fundamentals through memory, compilation, operating systems, concurrency, networking, performance, security, and systems capstones.',
  lessons: [
    lesson('machine-model', 'Bits, data representation, and the machine model', `Computers transform finite bit patterns under instructions. Binary and hexadecimal are compact notations; signed integers commonly use two's complement, while floating point represents a sign, exponent, and fraction with rounding and exceptional values. Bytes have addresses, multi-byte values have an endianness, and every type has a finite range and alignment. A C program is translated to machine instructions that move values among registers, memory, and devices.`, `Convert values by place weight, reason about masks and shifts, and calculate range before choosing a type. Treat integer overflow, truncation, sign conversion, padding, byte order, and floating comparison as explicit design concerns. Use fixed-width integer types when an external format requires exact width, size types for object sizes, and serialization routines rather than copying an in-memory struct onto disk or wire.`, `A bit pattern has no intrinsic type; interpretation supplies meaning. Signed overflow in C is undefined, floating arithmetic is not real arithmetic, and object representation may contain padding or trap-prone assumptions. Hardware, compiler, operating system, and language rules form separate contracts. Do not infer portability from one debugger view.`, `Encode and decode a versioned binary record containing a 32-bit counter, signed temperature, flags, and variable UTF-8 name.`, `Specify byte order and widths, write fields individually with bounds checks, reserve and validate a version, encode length before bytes, reject impossible length and unsupported flags, and compute expected total before reading. Test smallest and largest values, negative temperature, truncated input, extra fields, both host endian orders, and corrupt lengths. Never cast an unaligned byte buffer to the record struct.`, qs('machine representation', ['Bits gain meaning from an explicit type, width, byte order, and encoding contract', 'External formats should serialize fields rather than copy compiler-specific struct layouts', 'Finite ranges, rounding, alignment, and conversions can invalidate mathematical intuition', 'Mastery derives representations, validates boundaries, and proves portability with adversarial records'])),
    lesson('c-toolchain', 'The C toolchain and a disciplined first program', `A C source file passes through preprocessing, compilation, assembly, and linking. Headers declare interfaces, source files define objects and functions, object files contain code plus unresolved symbols, and the linker builds an executable or library. The implementation includes compiler, standard library, ABI, operating system, and target architecture. Warnings and debug information are part of the engineering loop.`, `Create a small project with separate modules, include guards, one declaration owner, a strict language standard, high warnings treated deliberately, debug and optimized profiles, dependency generation, tests, and reproducible commands. main returns a status to the environment. Check every fallible library call, print diagnostics to standard error, and keep normal machine-readable output on standard output.`, `The preprocessor performs token substitution, not typed computation. A declaration mismatch across translation units can compile and then misbehave. Undefined behavior lets optimization remove assumptions that seemed obvious in a debug build. Do not suppress warnings globally, include source files, hide ownership in macros, or rely on implicit declarations.`, `Build a command-line record inspector as three modules: format parser, report renderer, and program entry.`, `Put public types and declarations in narrow headers, internal helpers in source files, and compile every source separately under strict warnings, sanitizers, and debug symbols. Link one executable and one test binary. Feed bytes through the parser without global state, return structured errors to main, render only validated records, and expose exit codes for success, invalid input, and system failure. Verify a clean rebuild and optimized test run.`, qs('C toolchain', ['C programs are assembled from separately translated interfaces and definitions under an ABI', 'Strict warnings, explicit build profiles, and module boundaries make defects visible early', 'A successful debug compilation does not remove undefined behavior or interface mismatch', 'Mastery can explain and reproduce every preprocessing, compiling, linking, and execution artifact'])),
    lesson('types-control-functions', 'Types, expressions, control flow, and functions', `C provides arithmetic types, enumerations, pointers, arrays, structures, unions, and function types. Declarations bind a name to a type; expressions produce values and side effects. Operator precedence controls grouping but not every evaluation order. Blocks create scope and storage duration differs among automatic, static, thread, and allocated objects. Functions should communicate preconditions, results, and ownership through their interface.`, `Use explicit comparisons, braces, small functions, enum values for finite states, const for non-mutated access, and conversions only after checking range. Separate calculation from input and output. Return a status plus output parameter when every value of the result type is valid data, or use a tagged result structure. Keep loop invariants clear and prove index bounds from container length.`, `Short-circuit operators sequence their operands, but many other operand evaluations are unspecified. Modification and access without required sequencing can be undefined. switch falls through unless terminated; signed and unsigned mixing can produce surprising large values; recursion consumes finite stack. Avoid magic sentinel values when they collide with valid data.`, `Implement parsing and summary statistics for a bounded line of comma-separated integers.`, `Read a capped line, detect truncation, split without writing beyond the buffer, parse each token with an end pointer and range checks, and reject trailing junk. Maintain count, minimum, maximum, and a wider accumulator while proving overflow policy. An empty list is a distinct result. Keep parsing pure over a supplied span, let main own I/O, and test whitespace, limits, malformed signs, overflow, duplicate delimiters, and maximum count.`, qs('C functions and control flow', ['Types and function contracts should expose valid states, bounds, and ownership', 'Range checks must precede narrowing conversions and indexed access', 'Evaluation order, fallthrough, signed mixing, and sentinels can create hidden defects', 'Mastery writes small total functions and proves their behavior at every input boundary'])),
    lesson('pointers-arrays', 'Pointers, arrays, slices, and object lifetime', `A pointer identifies an object or position within an array and carries a pointed-to type. An array owns a fixed sequence of elements; in most expressions its name converts to a pointer to the first element, losing length. Pointer arithmetic is defined only within one array object or one-past it. Dereferencing requires a live suitably aligned object of the correct effective type.`, `Pass buffers as pointer plus element count and document whether null is allowed. Prefer half-open ranges and calculate remaining capacity before advancing. Use const pointers for read-only views and distinguish pointer constness from pointed value constness. Initialize every pointer, set obsolete owning pointers to an unusable state when helpful, and return references only to objects whose lifetime outlives the caller.`, `sizeof on an array parameter measures a pointer, not the caller array. One-past may be compared but not dereferenced. Comparing unrelated pointers for order, subtracting across objects, using a stack address after return, or retaining a pointer across reallocation is invalid. Pointer syntax is compact enough to conceal precedence errors, so decompose complex declarations.`, `Implement safe search, copy, and reverse operations over integer slices, including overlapping-copy policy.`, `Represent each slice as data plus length. Search reads indexes below length and returns a found flag with index. Reverse swaps mirrored indexes while left is below right. Copy first proves destination capacity and then either rejects overlap or deliberately uses overlap-safe movement; it never assumes a terminator. Tests cover zero length with null policy, one item, odd and even sizes, same buffer, partial overlap, insufficient capacity, and lengths near the arithmetic limit.`, qs('pointers and arrays', ['A pointer is valid only relative to a live object, permitted range, alignment, and type', 'Buffer interfaces need both address and length plus an explicit overlap and null policy', 'Array decay and reallocation can silently erase size or invalidate retained addresses', 'Mastery proves pointer provenance and bounds before every advance, comparison, and dereference'])),
    lesson('memory-ownership', 'Dynamic memory, ownership, and allocators', `Automatic storage ends at block exit; static storage lasts for the program; dynamic storage lasts from successful allocation until release. malloc returns uninitialized suitably aligned storage or null, calloc initializes bytes to zero, realloc may move an allocation, and free ends its lifetime. Ownership answers who releases an object, while borrowing grants temporary access without transfer.`, `Design constructors that establish invariants or fail without leaks, destructors that tolerate the documented empty state, and APIs whose names and types state transfer. Check multiplication before allocating arrays, retain the original pointer until realloc succeeds, centralize cleanup with one well-structured path, and pair every acquisition with release across every return. Use arenas or pools only when their bulk lifetime matches the domain.`, `Memory leaks exhaust long-running programs; double free, use-after-free, invalid free, uninitialized read, and buffer overflow corrupt execution and become security flaws. Reference counting cannot collect cycles without additional design. Zero-filled memory is not necessarily a valid instance of every abstract type, and clearing a pointer does not clear all aliases or secret bytes reliably.`, `Build a dynamically growing vector of owned strings with append, remove, reserve, clone, and destroy.`, `Store data pointer, length, and capacity with invariant length no greater than capacity. Reserve checks growth arithmetic, allocates or safely reallocates, and commits fields only after success. Append duplicates the input before publishing the new length. Remove frees the selected string, shifts remaining pointers, and decrements length. Clone rolls back every prior duplicate on failure. Destroy frees each string then the array and resets the container. Run allocation-failure injection at every allocation plus sanitizers over empty, growth, alias, and removal cases.`, qs('dynamic memory', ['Dynamic storage needs explicit ownership from successful allocation through one final release', 'Allocation arithmetic and state publication should be checked before committing mutations', 'Leaks, stale aliases, failed realloc, and partial construction violate lifetime invariants', 'Mastery survives injected allocation failure and proves cleanup on every control-flow path'])),
    lesson('strings-buffers', 'Strings, bytes, parsing, and defensive interfaces', `A C string is a byte sequence terminated by a zero byte; its capacity is not encoded. strlen searches for a terminator and cannot make an unterminated buffer safe. Text encoding such as UTF-8 maps characters to variable byte sequences, so byte count, code point count, displayed width, and grapheme count differ. Binary data may contain zero and must use explicit length.`, `Carry capacity with mutable buffers and length with views. Prefer bounded formatting that reports required size, validate its result, and use dedicated parsers with an end pointer and error channel. Define whether input is bytes, ASCII, UTF-8, or locale-dependent text. Reject embedded zero when adapting a length-delimited external field to a string API.`, `Functions with familiar names may truncate without termination, scan beyond bounds, depend on global locale, or overlap illegally. Escaping for shell, SQL, HTML, URL, and logs are different transformations; use structured APIs rather than generic replacement. A filename is not always valid UTF-8 and a line can exceed any guessed fixed buffer.`, `Write a configuration parser for key-value lines with comments, quoted values, escapes, and exact diagnostics.`, `Read with a size cap while detecting an overlong line, retain byte offsets, strip only specified line endings, and parse through a cursor bounded by end. Validate key grammar, decode allowed escapes into a separately sized output, reject duplicate keys according to policy, and never modify committed configuration until the whole file validates. Diagnostics include line, column, safe excerpt, and reason. Fuzz arbitrary bytes, embedded zero, incomplete escape, huge value, duplicate, invalid UTF-8 policy, and missing final newline.`, qs('C strings and buffers', ['C strings require a reachable terminator while binary spans require explicit length', 'Capacity, encoding, truncation, and output requirements belong in every buffer contract', 'A bounded function name alone does not guarantee termination, overlap safety, or correct escaping', 'Mastery parses with explicit cursors, caps, encodings, diagnostics, and unchanged state on failure'])),
    lesson('structs-bits-abi', 'Structures, unions, bit operations, and ABI boundaries', `Structures group named members and may contain padding for alignment. Unions share storage among alternatives but need a separate valid-tag discipline. Bitwise operators manipulate integer representations; masks select flags and shifts place fields. The application binary interface defines calling convention, object layout expectations, symbol naming, and linkage between compiled components.`, `Use tagged unions where the tag is set and checked before accessing the active member. Define flags as unsigned masks, validate unknown bits, and parenthesize operations. Expose opaque structures across module boundaries when representation may change. For files, devices, network packets, and foreign interfaces, translate fields explicitly to the external specification.`, `Compiler bit-field allocation, enum size, padding bytes, alignment, and packing extensions are not stable serialization. Reading an inactive union member or violating strict aliasing can be undefined. Packed objects can create unaligned access and performance or hardware faults. ABI compatibility can break when a public struct gains a field even if source recompilation would succeed.`, `Design a versioned plugin interface carrying commands and results between separately built components.`, `Expose an opaque context handle plus functions and a versioned table whose first fields are size and ABI version. Use fixed-width scalar fields and caller-supplied buffers at the boundary; never expose compiler-owned allocation without paired release. Check table size before reading optional later members, reserve flag bits, and translate tagged results explicitly. Test old host with new plugin, new host with old plugin, unsupported version, missing function, different optimization, and both shared-library unload and failure cleanup.`, qs('C data layout and ABI', ['In-memory layout includes implementation choices that are not automatically an external format', 'Tagged alternatives and opaque interfaces keep representation validity explicit', 'Packing, union punning, and public struct growth can violate alignment or binary compatibility', 'Mastery evolves a versioned boundary across separately compiled producers and consumers'])),
    lesson('compilation-linking', 'Preprocessing, linking, libraries, and program loading', `The preprocessor handles inclusion and conditional tokens before type checking. The compiler emits sections, symbols, and relocation records into object files. Static linking copies required library objects into an executable; dynamic linking resolves shared-library symbols at load or runtime. The loader maps program segments, prepares runtime state, and transfers control before main.`, `Keep headers self-contained, minimize exposed definitions, use internal linkage for private objects, and inspect symbol tables and dependency lists. Understand search paths, runtime loader configuration, position-independent code, and symbol visibility on the target. Pin a public ABI or rebuild dependents together. Record exact compiler, flags, target, and linked dependencies for reproducibility.`, `Header cycles and macro configuration can produce inconsistent structure definitions. Link order matters for some static archives. Uncontrolled runtime search paths permit dependency substitution. A dynamically loaded symbol cast or signature mismatch can corrupt calls. Link-time optimization crosses module assumptions and makes undefined behavior more surprising, not safer.`, `Diagnose a program that compiles but fails with duplicate symbols in one build and a missing shared library in another.`, `Use the linker map and symbol tool to find multiple external definitions, then keep one definition and declarations elsewhere or make true private objects internal. For runtime failure, inspect the executable's declared dependencies, loader search order, architecture, ABI version, and effective environment without copying random libraries into system paths. Package or configure a known dependency location, verify digest and exported version, and run under a clean environment. Add build tests that link every supported profile and packaged smoke tests that start the actual artifact.`, qs('compilation and linking', ['Object files carry sections, symbols, and relocations that a linker resolves into artifacts', 'Header discipline, visibility, search paths, and ABI policy control module integration', 'Compilation success cannot prove correct link identity or safe runtime dependency resolution', 'Mastery diagnoses symbols and loaders from artifacts rather than changing paths blindly'])),
    lesson('debugging-testing', 'Debugging, sanitizers, testing, and fuzzing', `Debugging turns a reproducible observation into a causal explanation. Begin with exact input, build identity, environment, expected result, actual result, and smallest reliable reproduction. A debugger controls execution and inspects frames, variables, registers, memory, and threads. Compiler sanitizers instrument undefined behavior, address use, memory leaks, and data races in supported configurations.`, `Build debug information with realistic optimization profiles, preserve the first failure, and reduce inputs without removing the cause. Write unit tests for pure modules, integration tests for operating-system boundaries, property tests for invariants, and fuzz targets for parsers accepting arbitrary bytes. Use assertions for impossible programmer states, not recoverable user input.`, `A changed variable in the debugger may be optimized away or stale. Memory corruption often appears after the bad write. Sanitizers change timing and do not cover every configuration; race detection requires exercised interleavings. Tests that compare only expected examples miss whole input classes. Logging secrets or enormous buffers during diagnosis creates another defect.`, `Investigate an intermittent parser crash that occurs only in an optimized batch job.`, `Capture the crashing artifact and exact binary, reproduce under the same limits, then run minimized input under address and undefined-behavior sanitizers. Enable core dumps where safe, inspect the first invalid access and allocation history, and bisect with evidence. Convert the minimized bytes into a regression and fuzzer seed. Add parser invariants, allocation-failure tests, and optimized sanitizer-compatible CI. Confirm the fix on the original workload and monitor crash signature rather than declaring success because one run passed.`, qs('systems debugging', ['Debugging starts from reproducible evidence and seeks the earliest violated invariant', 'Sanitizers, debuggers, properties, and fuzzers expose different defect classes', 'Timing changes, optimization, and delayed corruption can hide the original invalid operation', 'Mastery preserves artifacts, minimizes inputs, repairs cause, and adds lasting regression evidence'])),
    lesson('files-syscalls', 'System calls, files, descriptors, and robust I/O', `A process requests kernel services through system calls. File descriptors are small process-local references to open files, pipes, sockets, devices, or other objects. read and write may transfer fewer bytes than requested, be interrupted, reach end of file, or fail. The open file description holds offset and status shared across duplicated descriptors.`, `Loop on partial transfers, distinguish end of file from error, handle interruption according to operation policy, and close every descriptor on every path. Use restrictive creation permissions, safe path resolution, atomic replacement through a same-filesystem temporary file plus rename, and durability calls when the contract requires survival after crash. Buffering libraries add another state layer that must be flushed and checked.`, `A path can change between check and use, symbolic links can escape intended trees, descriptors can leak into children, and writing a file is not automatically durable. append semantics, locks, rename, and directory synchronization differ by filesystem and platform. Never assume one write is one record on a stream.`, `Implement a crash-aware state-file update and a streaming copy command with sparse error handling.`, `For copy, open source and destination with explicit policies, transfer through a bounded buffer while looping on reads and writes, preserve only requested metadata, and report close errors. For state update, create a unique restrictive temporary file in the target directory, write and validate complete bytes, flush file data when required, close, atomically rename, then flush the directory under the chosen durability contract. Refuse unsafe symlink traversal or operate relative to trusted directory descriptors. Test interruption, disk full, short writes, existing target, permission failure, crash points, and cleanup.`, qs('files and system calls', ['Kernel I/O may complete partially and file descriptors refer to state beyond a path name', 'Robust persistence requires explicit creation, transfer, replacement, cleanup, and durability policy', 'Path races, descriptor leaks, buffering, and filesystem semantics can invalidate simple examples', 'Mastery injects interruption, short transfer, disk exhaustion, crash, and restart at each boundary'])),
    lesson('processes-signals', 'Processes, execution, pipes, and signals', `A process has an address space, credentials, descriptors, environment, threads, and kernel state. Creating and executing a child separates process creation from replacing its program image. The parent must collect child status to avoid zombies. Pipes provide byte streams with finite buffers; closing unused ends is necessary for end-of-file and deadlock-free pipelines.`, `Build argument arrays directly rather than through a shell, set exact environment and working directory, close or mark unrelated descriptors, and define timeout, termination, output caps, and exit interpretation. Signal handlers can call only a restricted async-signal-safe set; commonly they set a flag or write to a self-pipe while normal code performs cleanup.`, `After process creation in a multithreaded program, the child has a fragile pre-exec environment. Shell interpolation creates injection. Parent and child retaining a pipe writer prevents EOF. A signal can arrive between checks, interrupt calls, or be coalesced. Termination does not guarantee grandchildren stop, so process groups or platform job controls may be required.`, `Run a compiler safely, capture bounded output, support cancellation, and return an exact result structure.`, `Accept executable and arguments separately, validate policy without shell quoting, create stdout and stderr pipes, start in a dedicated process group, close unused ends, and drain both streams concurrently under byte caps to avoid deadlock. Track monotonic deadline. On cancellation send graceful termination, wait a bounded interval, then terminate the complete group. Collect status exactly once and return start error, exit code or signal, timeout flag, and truncated outputs. Test a missing binary, huge dual output, inherited descriptor, child spawn, ignored termination, and cancellation race.`, qs('process control', ['Processes combine isolated address spaces with inherited credentials, descriptors, and environment', 'Safe child execution uses argument arrays, descriptor discipline, output draining, and bounded lifecycle', 'Shell parsing, retained pipe ends, signals, and descendants create injection or deadlock hazards', 'Mastery starts, observes, cancels, reaps, and classifies a complete process tree reproducibly'])),
    lesson('threads-sync', 'Threads, synchronization, and deadlock-free design', `Threads share process memory and descriptors while keeping stacks and execution state. A data race occurs when conflicting accesses to one memory location overlap without required synchronization and at least one is a write; in C this is undefined behavior. Mutexes create mutual exclusion, condition variables wait for a predicate change, semaphores count permits, and barriers coordinate phases.`, `State the invariant protected by each lock and use one mutex with its condition predicate. Check the predicate in a loop because wakeups and competing threads change state. Define a global lock order, keep critical sections bounded, transfer work through owned queues, and implement shutdown as state under the same synchronization. Join every joinable thread.`, `volatile does not make shared access atomic or ordered. Sleeping is not synchronization. Calling unknown or blocking code while holding a lock creates deadlock and latency. Cancellation can strand locks or partially changed state. False sharing harms performance even when correctness holds. Thread-local storage can hide lifecycle and cleanup problems.`, `Build a bounded producer-consumer work queue with graceful stop and no lost tasks.`, `The queue stores ring buffer, capacity, head, count, accepting flag, mutex, not-empty, and not-full conditions. Producers lock, wait while full and accepting, reject after closure, insert, signal not-empty, and unlock. Consumers wait while empty and accepting; when empty and closed they exit, otherwise remove and signal not-full. Shutdown locks, clears accepting, broadcasts both conditions, then joins workers. Tests force capacity one, many producers, spurious wakeups, close while blocked, task failure, zero work, and race-detector runs.`, qs('thread synchronization', ['Shared-memory correctness requires synchronization around stated invariants and conflicting access', 'Condition waits belong in predicate loops under the same mutex that protects state', 'Volatile, sleeping, and optimistic timing do not repair races or deadlocks', 'Mastery proves bounded queues, lock order, closure, wakeup, cancellation, and thread collection'])),
    lesson('atomics-memory-model', 'Atomics and the C memory model', `Atomic objects support indivisible operations and defined inter-thread ordering. The C memory model describes sequenced-before within a thread and synchronization relationships across threads. Sequential consistency supplies one global order for sequentially consistent atomics; acquire and release can publish prior writes through a matching observation; relaxed ordering guarantees atomicity but not unrelated visibility.`, `Start with mutexes. Use atomics only for a small documented protocol whose states, participating threads, and happens-before edges can be proven. Release-store a ready state after fully initializing data and acquire-load that state before reading it. Compare-exchange updates an expected value and may fail; loops must handle contention and memory order correctly.`, `An atomic flag does not automatically protect adjacent non-atomic data. Mixing atomic and non-atomic access to the same object is invalid. Relaxed counters can measure approximately but cannot publish structures. Lock-free does not mean wait-free, fair, faster, or immune to lifetime reclamation. ABA and freeing nodes while readers retain pointers make lock-free containers especially difficult.`, `Design a one-time immutable configuration publication and a relaxed diagnostic counter.`, `One initializing thread builds configuration privately, validates it, stores the pointer, then release-stores ready. Readers acquire-load ready before reading the stable pointer and never mutate the object. Lifetime extends until all reader threads stop, avoiding reclamation complexity. A separate atomic counter uses relaxed fetch-add because no other data depends on its value; reports treat it as diagnostic. Document why each ordering suffices and confirm with stress plus thread sanitizer where compatible. Prefer a standard once primitive if available.`, qs('C atomics', ['Atomicity and memory ordering are separate properties in a cross-thread protocol', 'Acquire and release can publish initialized data when their matching relationship is proven', 'Relaxed operations, lock-free claims, and atomic flags do not protect arbitrary nearby state', 'Mastery writes the happens-before proof and lifetime rule before choosing each memory order'])),
    lesson('os-memory-filesystems', 'Virtual memory, allocators, filesystems, and the kernel boundary', `Virtual memory gives each process an address space mapped by page tables to physical memory or backing storage. Pages carry permissions and faults let the kernel populate, copy, or reject mappings. Heaps acquire regions and allocators divide them into blocks. Filesystems map names through directories to metadata and data blocks, while caches defer physical I/O.`, `Read process maps, page-fault and resident-memory metrics, and filesystem metadata to understand actual use. Choose buffered I/O, memory mapping, or direct patterns from access and durability needs. Treat memory pressure, swap, copy-on-write, sparse files, hard links, symbolic links, mount boundaries, and permission identities as explicit semantics. Minimize privileged system calls and writable executable memory.`, `Virtual size is not resident use, free memory may be cache, and a mapped file can fault or truncate beneath a reader. Memory mapping does not eliminate validation or guarantee persistence. Deleting a directory entry does not necessarily remove open data. Container namespace views do not create a security boundary by themselves. Performance tuning based on one cache-warm run often reverses under pressure.`, `Compare buffered streaming and mapping for a large indexed file, including corruption, truncation, and memory pressure.`, `Define record format, index validation, expected random versus sequential access, file replacement rule, and maximum mapping. Benchmark cold and warm distributions while measuring faults, resident set, throughput, and tail latency. Buffered pread isolates offsets and handles short reads; mapping simplifies random access but every offset and length must be validated before touch and replacement must keep old mappings valid or be coordinated. Inject truncation, corrupt index, disk error, memory pressure, and concurrent replacement. Select from evidence and retain a safe fallback.`, qs('operating-system storage', ['Virtual addresses, pages, caches, and filesystem names are distinct layers of state', 'I/O design should follow access pattern, pressure, replacement, corruption, and durability contracts', 'Mapping, containers, and large virtual ranges do not remove validation or resource limits', 'Mastery explains observed faults, memory, cache, names, and disk behavior under failure'])),
    lesson('sockets-protocols', 'Sockets, protocols, and secure network programs', `A socket is an operating-system communication endpoint. Streams provide ordered bytes without message boundaries; datagrams preserve messages but may lose, duplicate, or reorder them. Name resolution maps service names and addresses under changing network state. A protocol defines framing, versioning, limits, state transitions, timeouts, errors, and authentication above transport.`, `Use length-prefix or delimiter framing with hard caps, loop on partial I/O, bound connections and per-peer work, and apply monotonic deadlines to connect, read, write, and idle phases. Handle multiple returned addresses. Protect transport identity with correctly verified TLS when the threat model requires it, then authorize application actions separately. Parse untrusted bytes incrementally into a validated state machine.`, `One send is not one receive. Endianness, structure layout, and text assumptions break interoperability. Slow peers consume descriptors and buffers; unbounded length fields allocate memory; retrying a request can duplicate side effects. Encryption without hostname or certificate validation authenticates nobody. Network errors often leave the remote operation outcome unknown.`, `Implement a concurrent length-prefixed key-value protocol with get and idempotent put.`, `Specify magic, version, request id, operation, key length, value length, and status in network byte order with strict maxima. A per-connection state machine reads exact header then body under deadlines. put includes an idempotency key stored with the resulting version; get returns value plus version. Authenticate peers, authorize key namespace, cap active connections and buffered bytes, and close cleanly on malformed frames. Test byte-by-byte delivery, coalesced frames, oversized and truncated lengths, slow client, duplicate put, timeout after commit, invalid certificate, and restart.`, qs('network systems', ['Stream transports carry bytes, so application protocols must define framing and lifecycle', 'Every connection and field needs bounded size, time, concurrency, identity, and authorization', 'Partial transfer, slow peers, invalid verification, and unknown outcomes defeat naive socket code', 'Mastery specifies, implements, fuzzes, overloads, secures, and recovers a versioned protocol'])),
    lesson('performance-security', 'Systems performance, exploitation classes, and hardening', `Systems performance is constrained by computation, memory hierarchy, allocation, locks, system calls, storage, or network. Measure throughput, tail latency, CPU time, wall time, faults, cache behavior, I/O, contention, and resource saturation under a representative workload. Security failures commonly arise from out-of-bounds access, lifetime errors, integer mistakes, format strings, injection, unsafe permissions, and confused trust boundaries.`, `Profile before optimizing, preserve correctness tests, and change one hypothesis at a time. Improve algorithms and data layout before micro-tuning. Compile with relevant warnings and platform hardening, use non-executable memory, address randomization, control-flow defenses where available, least privilege, sandboxing, safe temporary files, and strict parsing. Treat dependencies and build artifacts as supply-chain inputs.`, `A fast benchmark can hide coordinated omission, cache warmth, small data, or disabled validation. Bounds checks rarely justify removal without evidence. Undefined behavior can become exploitable under optimization. Mitigations reduce exploitability but do not correct the defect. Secret-dependent branches and memory access can leak timing even when outputs are correct.`, `Harden and optimize a binary record server without weakening its validation or isolation.`, `Establish a workload and latency objective, fuzz and sanitize the parser, then profile CPU, allocations, locks, faults, and network. Replace repeated allocation with bounded reusable buffers, improve index locality, and shard locks only after invariant review. Keep all length and overflow checks, authenticate before expensive work, rate-limit identities, drop privilege, restrict filesystem and network access, enable compiler and loader hardening, and redact logs. Re-run functional, fuzz, sanitizer, race, exploit-regression, and cold and warm load tests after every optimization.`, qs('systems performance and security', ['Optimization and hardening both begin with explicit workload, threat, and correctness models', 'Algorithms, layout, bounded resources, isolation, and compiler defenses reinforce one another', 'Benchmark speed or mitigation presence cannot excuse undefined behavior and unsafe parsing', 'Mastery measures, exploits safely, repairs root causes, hardens boundaries, and retests performance'])),
    lesson('capstone-systems', 'Capstone: build and operate a systems toolkit', `Complete three integrated projects from a clean build. First, write a small Unix-like shell with parsing, pipelines, redirection, process groups, status, cancellation, and no unsafe interpolation. Second, implement an arena plus debug allocator that tracks ownership, alignment, overflow, failure injection, and leak reports. Third, build the versioned authenticated record server and client from the networking lesson with persistent atomic state.`, `Specify every grammar, binary format, API, ownership rule, state machine, permission, resource cap, timeout, and exit status before coding. Organize modules with self-contained headers and opaque state. Provide strict debug, sanitizer, race, optimized, and hardened profiles. Tests include pure parsing, real filesystem and process integration, protocol fuzzing, concurrency barriers, golden compatibility, and restart recovery.`, `Attack every project locally with malformed syntax, extreme lengths, allocation failure, partial I/O, signals at each phase, child descendants, descriptor exhaustion, slow peers, duplicate requests, corrupt files, disk full, concurrent updates, and incompatible versions. Measure representative cold and warm performance. A clean happy path is not completion; another operator must diagnose and recover it from artifacts and documentation.`, `Write one invariant ledger and failure matrix covering memory, file, process, thread, and network boundaries across all three projects.`, `For each resource record creator, owner, borrower, valid states, release, error propagation, concurrency protection, and observable evidence. For each operation enumerate failure before mutation, during partial mutation, after commit but before acknowledgement, and during cleanup. Connect cases to a deterministic test, sanitizer or fuzzer, log or metric, and recovery step. The toolkit passes when a clean machine can build it reproducibly, compatibility tests cross versions, injected failures leak no resource or privilege, the server restores valid state, and measured optimization retains every safety property.`, qs('computer-systems mastery', ['Systems mastery integrates language rules, operating-system semantics, resources, and threat models', 'Every acquired resource needs ownership, bounded lifecycle, error evidence, and tested release', 'A successful execution cannot prove behavior under partial I/O, races, exhaustion, or restart', 'Mastery specifies, builds, fuzzes, profiles, attacks, operates, and recovers complete native programs']))
  ]
}
