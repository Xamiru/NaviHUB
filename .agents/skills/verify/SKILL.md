---
name: verify
description: "LAPTOP ONLY. Launch and drive the built NaviHUB Electron app for end-to-end verification (screenshots, navigation) against the real library DB. Use this to confirm a renderer change actually works before reporting it done. Do NOT invoke on the VPS — it is headless and has no library."
---

# Codex adapter

Read [the authoritative Claude workflow](../../../.claude/skills/verify/SKILL.md) completely before taking action, then follow it as the workflow for this skill.

The files under `.claude/skills/verify/` remain authoritative, including any scripts or assets they reference. When that workflow says to read or update `CLAUDE.md`, treat the instruction as applying to both byte-identical root contracts, `CLAUDE.md` and `AGENTS.md`.
