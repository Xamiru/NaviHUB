# Japanese learning vertical

> Reference detail. The rules an agent must not break live in
> [CLAUDE.md](../../CLAUDE.md#hard-invariants) — this file is the "how and why" narrative.

**Covers** — data packs, the drill catalogue, ghost reviews and the i+1 feed, onboarding, and the on-screen kana keyboard.

**Key files** — `src/main/dict/`, `src/main/jpDrills.ts`, `src/main/jpFeed.ts`, `src/main/jpConfusables.ts`, `src/main/repos/japaneseRepo.ts`, `@shared/srs.ts`, `components/japanese/`

**Tests** — `japaneseRepo`, `jpDrills`, `jpFeed`, `jpGhost`, `jpConfusables`, `srs`, `dict*` (18 files), `kanaKeyboard`, `imeCompose`

---

## Wave 2 — packs and drills

**Japanese wave 2 (2026-08-01)** — five new data packs, eight new drills, and a TheMoeWay-shaped roadmap. **Packs** (all in `dictionaries.db`, additive `CREATE TABLE IF NOT EXISTS` — NO DICT_SCHEMA_VERSION bump; every new child table is in `sweepOrphans` AND the version-bump DROP list, kept honest by tests/dictSchemaSync.test.ts which diffs init.sql against both lists): **Kanjium pitch** (`dict/kanjium.ts`, sha-pinned raw TSV → the EXISTING `pitch` table under a `dict` registry row titled `Kanjium Pitch Accents` — `loadPitches` has no dict filter, so the dictionary page's PitchAccent rendering lit up with zero lookup changes; `listDictionaries` gained a `pitch_count` subselect + `DictInfo.pitchCount`); **KRADFILE components** (`dict/krad.ts`, sha-pinned krad-unicode JSON → `krad_set`/`krad`/`krad_part` (the radkfile inversion, computed at import so search-by-parts is one GROUP BY/HAVING)/`krad_component`; `KanjiInfo.components` rides every kanji lookup); **grammar N5-N1** (`dict/grammar.ts`, five sha-pinned hanabira.org JSONs → `grammar_bank`/`grammar_point`; **cloze fields pre-computed at import** via `@shared/cloze.ts:grammarPointCandidates`/`clozeGrammarExample` so the drill pool filter is `clozeJp != null`); **JMnedict names** (a PRESETS entry through the normal Yomitan pipeline with new `importFromReader` opts `{glossFts:false, defaultPriority:-10}` — skipping gloss_fts is THE English-search pollution fix (searchEnglish ranks by FTS score only) and priority −10 sinks name groups below words; `DictEntry.isName` is computed in lookup.ts from the dict title (a registry column would force a version bump that wipes installed dicts), and the same commit fixed the latent defs-order bug: group defs now sort by dict PRIORITY, not id, so JMnedict never outranks JMdict inside a merged group like 東京; `dict/names.ts:nameSample` aggregates ALL readings per expression); **audio** — first audio in the section, under the new `jpaudio/` navimg prefix (`files.ts:jpAudioDir()`, always userData): **minimal pairs** (`dict/minimalPairs.ts`, sha-pinned codeload zip of Kuuuube/minimal-pairs; per-pair JSON with BASE64 audio decoded to `jpaudio/pairs/`, container sniffed by magic bytes — the pack ships ADTS AAC, not mp3) and **Tatoeba sentence audio** (`dict/tatoebaAudio.ts` + runtime dep **`seek-bzip`** (pure-JS bz2; typed in src/main/env.d.ts; verify `grep 'import("seek-bzip")' out/main/index.js`): two bz2 exports → plan → ~6k throttled per-clip fetches (300ms, single worker, `rateLimitWaits:0`, abort after 20 consecutive failures), **resumable** — clips are keyed `jpaudio/tatoeba/<audio_id>.mp3` and existsSync-skipped on re-run; rows join the sentence bank BY EXACT JP TEXT so bank re-imports never orphan them; empty license column = clip skipped, per-clip license/attribution stored and rendered; `SentenceExample.audioPath` joins in via a subselect in `sentences.ts:querySentences`; the Settings poll now self-gates on `status.running` (not just local busy) so the ~20-min run survives navigation, and BOTH dictionary cards disable while ANY import runs). **Drills** (QuizKind += `pitch pairs components grammar names numbers dictation shiritori`; every pack-gated setup degrades to EmptyState→Settings; audio drills use a page-local `new Audio(mediaUrl(...))` — NEVER the global player queue — plus a one-shot courtesy `player.toggle()` if music is playing): `/japanese/pitch` (Patterns = contour MC over `japanese:pitchQuizPool`, cards×Kanjium with freq top-up; Minimal pairs tab = kotu mechanic, 1/2 answer, R replay, endless log-on-Stop), `/japanese/listen` (dictation: graded on kuromoji READING keys via `@shared/dictation.ts`, raw-text fallback when tokenize returns []; per-char LCS diff on reveal), `/japanese/kanji` (by-parts browser) + `/japanese/kanji/quiz` (build-a-kanji over `japanese:componentQuizPool` — kanji-KIND lessons, not the title heuristic; decoys stroke-biased in main), `/japanese/grammar` (library: level pills + client-side search over ONE cached `dict:grammarList`; `?q=` deep link; grammar lessons link in via `grammarCandidates(title)`) + `/japanese/grammar/quiz` (cloze MC), `/japanese/shiritori` (validation = `dict:lookup` exact-form; replies = `dict:shiritoriNext`, freq-jittered nouns, JMnedict excluded via `priority >= 0`; logs tournament-style score=total=chain), and JapaneseKanaPage ("Typing Drills") grew **Numbers & counters** (endless per-keystroke over `@shared/numbers.ts` — the euphonic tables ARE the feature, exhaustively tested — checked by `@shared/typing.ts:matchState`, whose invariant is never-'wrong'-while-a-completion-exists) and **Names** tabs, with `?tab=` seeding (also on /japanese/pitch). The generic Drill engine moved VERBATIM to `components/japanese/TypedDrill.tsx` (KanaDrill stays in-page). The practice quiz is now **multi-direction** (`directions: Direction[]` under NEW key `jpQuizDirections`, default `['jp2en']` preserves old behavior; per-question random supported direction; sub-pools per direction, <4-distinct-answer directions silently dropped; kind stays `'japanese'`). Cross-DB pools live in `src/main/jpDrills.ts` (coreDeck pattern); pools are plain awaits at Start, never cached. **Roadmap** gained the Daily-loop strip (`RoadmapDailyLoop` — reviews/lesson/immersion; the immersion tile's query must stay byte-identical `{ mediaType: 'manga' }`, shared qk.media.home cache) and Milestones (`RoadmapMilestones` over `media:jpMilestones` — `mediaRepo.jpMilestones()` resolves completed POSITIONALLY via `@shared/mediaProgress.parseStatuses`; novels = manga-type completed with an `.epub` manga_chapter; `qk.media.jpMilestones` sits under the `['media']` prefix on purpose). No sanitizeSql changes anywhere: dictionaries.db and `jpaudio/` are not exported, quiz_session was already wiped. Pinned shas live as constants in each dict module; JMnedict deliberately uses `releases/latest` like the other presets.

## Wave 3 — confusables, i+1 feed, ghost reviews

**Japanese wave 3 (2026-08-02)** — Reddit-mined (r/LearnJapanese + WaniKani/Bunpro forums): confusable discrimination, an i+1 reading pipeline, the SRS mechanics every community reinvents, and local pitch production feedback. **Zero new data packs; ONE new table `jp_ghost`** (init.sql + schema.ts + sanitizeSql wipe). QuizKind += `lookalike transitivity homophone loanword keigo leech speak`. **Shared pure modules** (each exhaustively tested): `similarKanji.ts` (component-jaccard 0.55 + stroke 0.30 + shared-onyomi 0.15; `SIMILAR_OVERRIDES` hand list because kradfile can't see primitive pairs like 人/入), `transitivity.ts` (89 pairs; past-form surfaces DERIVED via conjugate() so typos can't ship; key = kanji forms — kana collides 直る/治る), `keigo.ts` (18 suppletives + 40 regulars; masu-stem = `conjugate(kana,cls,'masu').slice(0,-2)`), `confusables.ts` (`confusableTier` = tier-0 quiz distractors per direction: partner→homophone→shared-kanji; jp2en = partner ONLY), `pitchTrack.ts` (hand-rolled YIN F0 tracker + `targetLevels` — EXTRACTED from PitchAccent.tsx which imports it back; odaka≡heiban in isolation is pinned honest). **Main**: `dict/similarKanji.ts` (on-demand `krad_part` component-IN query, NO precomputed table), `jpDrills.ts` grew lookalike/homophone/transitivity/loanword pools (homophones group by toHiragana(reading) over freq≤30000; transitivity FTS-matches a real sentence requiring exactly-one-動詞-token + other-member-absent, authored fallback so the drill needs no pack; componentQuizPool decoys now prefer look-alike components), `jpConfusables.ts` (lapse-driven pair detector: exact reading / shared kanji / component-jaccard≥0.5), `jpFeed.ts` (the flagship i+1 feed — **two-phase because `sentence_fts.keywords` unions BASE+SURFACE forms**: coarse keyword band [1..3] as upper bound → exact kuromoji pass where known iff base∈set||surface∈set, pinned by regression test; module cache keyed by params + knowledge fingerprint card-count|max-updated|learned-count). **Ghost reviews**: spawn inside `submitReview` on review-status Again (GHOST_STEPS=3, ON CONFLICT reset), `ghostQueue` excludes currently-due cards, `ghostAnswer` decrements/dissolves and **writes NOTHING to jp_review_log** (stats purity — heatmap/streak unaffected, documented tradeoff); cleaned up in removeCard/resetCard/removeLesson/removeCourse. **Stats**: retention strict=Good+Easy (30d + all-time), forecast → 30 days w/ 7/14/30 pills, Journey block (distinct cards/words mined/lessons/chapters/quiz rounds — all derived, never hand-logged per AJATT's own verdict), leech "Drill these" → `/japanese/leeches/drill` (flip self-check, NO SRS writes), "Possibly confused" pairs w/ reason chips. **Backlog cap** = renderer `due.slice(0, cap)` (repo orders most-overdue-first; NO postponement writes). Lateness IS scheduled for: both `submitReview`s select `MAX(0, julianday('now') - julianday(due_at)) AS overdue_days` and pass it to `gradeCard` as `elapsedDays`, which uses `max(intervalDays, elapsedDays)` as the Good/Easy multiplier base (Hard deliberately excluded — the answer says the gap was too long). `gradeCard` also takes an injectable `rng` and fuzzes review intervals ±5% (`FUZZ_RATIO`), because `buildCoreDeck`/`buildPrepDeck`/`setLessonLearned` all introduce cards in cohorts; `previewIntervals` pins the rng to its midpoint so the buttons never flicker. A lapse keeps `LAPSE_NEW_INTERVAL` (0.4) of the old interval rather than dropping to 1 day, and relearning Easy applies `EASY_BONUS` instead of being byte-identical to Good. Coverage: `projection` rows ("learn top 20 → 94%") share the tier denominator, `lookupInterval` line, coverage page sort pills incl. Closest-to-my-level (|knownShare−0.95|). **Pages**: `/japanese/confusables` (tabs lookalike/pairs/homophones over shared `McDrill.tsx` render-prop MC loop), `/japanese/loanwords` (MC-4 glosses; distractors deduped by first-gloss-token), `/japanese/feed` (One-new-word/All-known pills, ▸ translation disclosure, inline mine via useMiningDraft, staleTime Infinity), `/japanese/leeches/drill`, keigo = 6th JapaneseKanaPage tab, **Speak = 3rd pitch-page tab**: `lib/usePitchRecorder.ts` (ScriptProcessor — AudioWorklet breaks on file:// + CSP has no blob:; raw constraints, echo/noise/AGC OFF; NO permission handler added — Electron default grants, and any future handler must keep 'fullscreen'), `PitchContourChart.tsx` (SVG target steps + semitone polyline), `SpeakDrill.tsx` (R record 6s cap, grades vs EVERY attested position reports best, honest shape-only copy). Review setup: due-cap pills + ghost toggle (both usePersistedState — SettingsPage untouched this wave). Practice quiz distractors: `confusableTier` prepended as tier-0. Dictionary page: similar-kanji chips per kanji row + transitivity-partner callout.

## Onboarding

**Japanese onboarding (2026-08-04)** — the user is now actively learning (kana done, ~1h/day, manga/VN reading goal); this wave made the section's PROCEDURE visible, engine untouched. (1) **`SetupChecklist`** (`components/japanese/SetupChecklist.tsx`, on the hub between Continue and Study): the "core six" packs (JMdict → frequency → KANJIDIC → KRADFILE → grammar → sentence bank) with live installed-state — predicates MIRROR SettingsPage (`title.startsWith` for Yomitan packs, `freqCount > 0` for frequency, nullable info invokes for the rest; keep in step) — renders null until all four queries succeed AND null once all six are installed. (2) **SettingsPage now seeds its tab from `?tab=`** (MediaDetailPage one-shot idiom; the checklist links `/settings?tab=japanese` — tab key `'japanese'` is frozen). (3) Roadmap gained a static **`KanaStepRow`** ("Step 00", links `/japanese/kana`) with an honest "Practiced" (never "Cleared") chip off `qk.quiz.history('kana')`. (4) Sidebar Japanese children += Guide; CommandPalette += kana/mine/quiz/test/dictionary/loanwords/grammar-quiz/kanji-quiz. (5) Guide's "A day" card grew "One hour" + "Starting out (kana done)" dt/dd entries. (6) Daily-loop tile 03 falls back to `/japanese/feed` (not bare `/manga`) when no manga is in progress — the media query itself is still byte-identical. (7) **`JpStats.introducedToday`** (cards whose FIRST `jp_review_log` row is today, localtime — ghosts write no log so can't inflate) shown as an advisory under the review setup's new-cards pills; no new IPC channel, field rides `japanese:stats`.

## On-screen Japanese keyboard

**On-screen Japanese keyboard (2026-08-12)** — `JpKeyboardInput` (`components/japanese/keyboard/`) is a drop-in for `<input className="input">` at JP-typing sites (v1: dictionary search, shiritori, dictation — deferred: mining/lesson-form fields, textareas): an あ toggle opens a panel with three switchable layouts (gojūon chart rendered COLUMNS right-to-left あ-rightmost, flick 12-key with drag petals in `FlickKey.tsx`, romaji QWERTY) + an IME candidate bar. Pure parts: `@shared/kanaKeyboard.ts` (layout tables + dakuten/handakuten/small toggles + the phone ゛゜小 `cycleKana` orbit base→small→dakuten→handakuten) and `@shared/imeCompose.ts` (`ComposeState {kana, pending}` — typing.ts's interpret() promoted; re-converts the whole pending tail per keystroke, holds a lone trailing "n"; no-op returns the SAME object). **There is no katakana mode** — the engine composes hiragana and the candidate row leads with the two script chips (hira + `toKatakana`), which is also the empty-dict degradation. Candidates: `dict:readingCandidates` (`dict/imeCandidates.ts`) — exact reading match (both scripts, `d.priority >= 0` excludes JMnedict, dedupe by expression, rank freq→common-tag→dict-priority→score, script-form expressions EXCLUDED as chip duplicates) then freq-GATED LIKE-prefix predictions (≥2 kana only), cap 16, `[]` on any failure; renderer queries it via `qk.dict.imeCandidates` on a 150ms-debounced `commitText`. Load-bearing renderer details: EVERY panel control `preventDefault`s pointerdown (focus must never leave the host input), caret restored via rAF `setSelectionRange` after controlled onChange, physical typing bypasses the buffer entirely (goes straight to the input) EXCEPT Enter/Escape, which route through the `JpKeyboardBridge` ref first — Enter with uncommitted kana COMMITS it (never submits the stale value), Escape clears the buffer, then closes the panel, then reaches page cascades (`stopPropagation`). Prefs (layout + romaji hints via `kanaToRomaji` sublabels) in localStorage `jp.keyboardPrefs` (book.readerPrefs idiom). This wave also extended romaji.ts BASE/DIGRAPHS (x/l small kana, ゔ/`vu`+combos, ファ/ティ/ウィ/シェ families) — app-wide effect on `romajiToHiragana`/`acceptedRomaji`, safe because matchState's documented failure direction is "too lenient". Tests: kanaKeyboard (completeness/orbits), imeCompose (keystroke-vs-full-string equivalence), dictIme (real dict schema). GUI smoke pending on the user's machine (headless box).

