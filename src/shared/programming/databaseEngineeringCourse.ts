import type { ProgCourseDef } from './types'
import { masteryLesson as lesson, masteryQuestions as qs } from './masteryCourse'

export const DATABASE_ENGINEERING_COURSE: ProgCourseDef = {
  key: 'database-engineering',
  title: 'Database engineering: beginner to master',
  description:
    'A self-contained path through relational modeling, SQL, storage engines, indexes, optimizers, transactions, schema evolution, replication, recovery, security, and database capstones.',
  lessons: [
    lesson('relational-model', 'Relations, keys, constraints, and data grain', `A relational database represents facts as relations with named attributes and tuples. A table design begins by stating its grain: exactly what one row means. Candidate keys uniquely identify that fact, a primary key is the chosen candidate, and foreign keys connect valid identities. Domains, nullability, unique constraints, checks, and references make invalid states harder to store.`, `Translate requirements into entities, values, relationships, lifecycle, and invariants before creating columns. Separate facts with different grains. Use stable surrogate keys when natural identity is wide or mutable while retaining natural uniqueness as a constraint. Model many-to-many relationships through a relation whose key and attributes reflect the relationship. Normalize repeated and dependency-driven facts, then denormalize only with a refresh and reconciliation contract.`, `Null can mean missing, inapplicable, or unknown, which have different domain meanings but one SQL marker. A foreign key does not prove authorization or temporal validity. Generic entity-attribute-value tables trade schema guarantees for runtime complexity. Storing comma-separated identities prevents referential integrity and useful query planning.`, `Model users, organizations, memberships with roles, media titles, personal tracking, and immutable progress events.`, `Give each table one grain: organization, user, one membership per organization and user, canonical media, one current tracking row per membership and media, and one progress event per observed action. Constrain membership and tracking uniqueness, role domain, score range, event idempotency identity, and references. Current tracking can be transactionally maintained from events or derived; never reconstruct an old event from today's mutable title or role. Document deletion, retention, and cross-organization access rules.`, qs('relational modeling', ['Every relation needs one declared row grain and keys that preserve that fact', 'Constraints place durable domain rules at the shared concurrency boundary', 'Nulls, generic attributes, and delimited identities can erase essential semantics', 'Mastery derives schema, invariants, lifecycle, history, and query needs from concrete facts'])),
    lesson('sql-querying', 'SQL querying from single tables to analytical windows', `SQL is declarative: describe the result relation while the engine chooses an execution plan. SELECT forms a logical pipeline of source, joins, filtering, grouping, aggregate filtering, projection, distinctness, ordering, and limiting, although physical execution may differ. Three-valued logic means predicates involving null can be unknown. A result order exists only with an explicit complete ORDER BY.`, `Use explicit columns, qualified names, intentional inner or outer joins, and parameterized values. Aggregate at the stated grain and understand how joins multiply rows before aggregation. Common table expressions name logical stages. Window functions calculate rank, running values, lag, and partition aggregates without collapsing rows. Cursor pagination orders by a stable unique tuple and carries the last tuple forward.`, `NOT IN with a null can surprise; use NOT EXISTS for anti-joins with clear correlation. Filtering the nullable side of an outer join in WHERE can turn it into an inner join. LIMIT without stable order is nondeterministic. Dynamic identifiers cannot be parameterized like values and require a fixed allowlist.`, `Write one query returning each organization member, current title count, last activity, and rank within organization, including inactive members.`, `Start from membership as the required grain. Pre-aggregate current tracking by membership and progress events by membership separately, then left join those one-row summaries so counts do not multiply. Coalesce count to zero but leave missing activity semantically null. Apply row_number or dense_rank partitioned by organization and ordered by count descending, activity with explicit null placement, and membership id as tie-breaker. Scope the organization in every relevant subquery and inspect results for empty, tied, and duplicate-event cases.`, qs('SQL querying', ['SQL operates on relations and has a logical evaluation model distinct from physical execution', 'Pre-aggregation and explicit grain prevent joins from multiplying measures', 'Null logic, outer-join filters, and incomplete ordering can silently change results', 'Mastery writes correct relational stages and verifies their plan and edge-case result shape'])),
    lesson('storage-pages', 'Storage engines, pages, records, and write paths', `A storage engine organizes tables and indexes into fixed-size pages managed through a buffer cache. Pages hold headers, slot arrays, records, free space, and links or tree positions. Variable records may live inline, overflow, or indirect. A write changes cached pages and durable log records according to the engine's recovery protocol; it is not normally one immediate disk overwrite per row.`, `Understand heap organization, clustered organization, page splits, free-space maps, visibility metadata, and row-version layout for the chosen engine. Keep rows reasonably narrow, place large values deliberately, and estimate working set from row and index bytes rather than row count alone. Monitor cache hit with latency and I/O, dirty pages, checkpoint behavior, log generation, and storage amplification.`, `Logical rows do not map permanently to one physical address. Updates may create new versions, forwarding, fragmentation, or index maintenance. Page fullness optimized for reads may increase split cost under random inserts. Compression saves I/O but spends CPU. The operating-system cache and database cache can interact. Do not infer durability from a file timestamp.`, `Estimate and observe storage for an append-heavy event table plus a frequently updated current-state table.`, `Define each row's actual encoded columns, null overhead, alignment, and index entries, then multiply by realistic volume and versions. Partition events by lifecycle only if operations need it; keep append order favorable for locality and retention. Current state uses a compact primary access path and avoids indexing frequently changed low-selectivity fields. Load representative data, inspect page and relation statistics, perform updates and retention, vacuum or compact under documented engine behavior, and compare predicted to observed table, index, log, and backup growth.`, qs('database storage', ['Database rows and indexes live in managed pages with caching, versions, and recovery metadata', 'Capacity estimates must include encoded records, indexes, churn, logs, and maintenance space', 'Logical updates can create splits, versions, fragmentation, and write amplification', 'Mastery connects schema choices to observed pages, I/O, cache, checkpoint, and storage growth'])),
    lesson('indexes', 'Indexes and access paths', `An index is an auxiliary structure mapping key values to row locations or included data. B-trees support equality, ordered ranges, prefixes, and ordered traversal. Hash indexes target equality under engine-specific rules. Bitmap, inverted, spatial, columnar, and vector structures serve different predicates. Every index accelerates selected reads while consuming storage and write work.`, `Derive an index from a real query: equality prefixes, then useful range or order, with included columns only when they avoid expensive lookup. Make uniqueness a constraint when it is an invariant. Partial indexes cover a stable subset; expression indexes require the query to match the expression. Composite order matters, and a low-cardinality leading column may be weak unless combined with a selective pattern.`, `Indexing every column slows writes and maintenance. A function, cast, collation mismatch, wildcard prefix, or stale statistic can prevent an expected access path. Covering indexes duplicate data and lose benefit when visibility checks still require table access. An index can support filtering yet still return too many rows to beat a scan.`, `Design indexes for tenant-scoped title lookup, recent activity pagination, unique active invitation, and unfinished-job claiming.`, `Use organization plus normalized title for tenant lookup if search semantics fit B-tree prefixes, otherwise choose a text-search structure. Recent activity uses organization, occurred time descending, and unique event id for cursor order, with narrow included display fields if measured. Enforce one active invitation through the engine's supported conditional uniqueness design. Job claiming indexes state, next-attempt time, priority, and stable id while excluding terminal rows. Load realistic skew, update statistics, inspect plans, measure write cost, and remove redundant prefix indexes only after dependency review.`, qs('database indexes', ['An index is a workload-specific access path with storage and write-maintenance cost', 'Key order should follow equality, range, ordering, selectivity, and stable pagination needs', 'Functions, casts, skew, stale statistics, and low selectivity can defeat an apparent index', 'Mastery derives, measures, validates, and retires indexes against representative query distributions'])),
    lesson('query-planning', 'Query optimizers, plans, and performance diagnosis', `A query optimizer rewrites relational expressions, estimates row counts and costs, chooses join order, access paths, algorithms, parallelism, and memory, then produces a plan. Nested-loop, hash, and merge joins fit different sizes and order. Estimates come from statistics and assumptions; actual runtime depends on cache, parameters, concurrency, and resource limits.`, `Begin with the slow user operation and capture query text or fingerprint, parameter shape, plan, actual rows and time where safe, waits, I/O, memory, and frequency. Compare estimated with actual cardinality at each node. Fix incorrect semantics or missing constraints first, then statistics, indexes, query shape, data model, or resource configuration. Test cold and warm representative cases.`, `One fast plan can fail for a skewed parameter. Parameter-sensitive planning, correlated predicates, temporary tables, functions, and stale stats cause misestimation. Forcing a plan can fossilize a temporary fix. EXPLAIN without execution shows estimates, while execution analysis may run a destructive statement. Faster latency at one concurrency can produce worse total throughput.`, `Diagnose an organization report that is fast for small tenants and times out for one very large tenant.`, `Collect both parameter cases and compare actual to estimated rows before and after each join. Inspect tenant-size skew, correlated status and time filters, sort spill, repeated lookup, and lock waits. Consider richer statistics, a composite tenant-first access path, pre-aggregation, or a separate strategy selected by size only when supported and tested. Bound output and paginate. Benchmark mixed concurrent traffic so the large-tenant fix does not starve ordinary users, then monitor plan identity, rows, spill, and latency by bounded tenant-size class.`, qs('query optimization', ['Optimizers choose physical work from estimates, statistics, costs, and available algorithms', 'Estimated-versus-actual rows reveal where a plan model first diverges from reality', 'One parameter, forced plan, or warm test cannot represent skewed production workloads', 'Mastery traces user latency through plans, waits, I/O, spill, concurrency, and durable remediation'])),
    lesson('transactions', 'Transactions, locking, and atomic business changes', `A transaction groups reads and writes into one commit or rollback under an isolation level. Atomicity means partial transaction effects do not commit; consistency is supplied jointly by correct transactions and constraints; isolation controls interaction among concurrent transactions; durability defines survival after acknowledged commit under the configured system. Locking engines coordinate resources and MVCC engines preserve versions, often using both.`, `Keep transactions focused around one durable invariant, acquire resources in consistent order, and make external calls outside the database transaction with a workflow or outbox. Use conditional updates and unique constraints to decide races at the database. Handle deadlock or serialization abort by retrying the complete transaction only when the operation is idempotent and within a bounded budget.`, `Read then write in separate transactions creates time-of-check races. Long transactions retain locks or old versions and delay cleanup. A committed database transaction cannot atomically include an ordinary network call. Autocommit can split a multi-statement invariant. Retrying only the last statement after an isolation abort uses stale decisions.`, `Implement a limited-seat reservation with idempotent request handling and expiration.`, `In one transaction insert or retrieve an idempotency record, lock or conditionally update the event inventory, insert the reservation with expiry and unique request identity, and store the response. If capacity cannot update, return sold out without a reservation. Expiration is a separate idempotent transition that returns capacity only if the reservation is still pending. Payment and email use outbox events after commit. Race more buyers than seats, retry after unknown acknowledgement, deadlock deliberately, expire concurrently with confirmation, and prove seats never go negative or return twice.`, qs('database transactions', ['Transactions make a bounded set of database decisions commit or roll back together', 'Constraints, conditional writes, and complete retries resolve concurrency at the durable boundary', 'Long scope, split autocommit, and external calls inside transactions create blocking or dual writes', 'Mastery proves business invariants under races, aborts, unknown acknowledgement, and replay'])),
    lesson('isolation-mvcc', 'Isolation levels, MVCC, and concurrency anomalies', `Isolation defines which interleavings appear possible. Read committed commonly prevents dirty reads but permits changing results between statements. Repeatable-read and snapshot variants preserve a transaction snapshot but can differ on phantom and write-skew behavior. Serializable isolation aims to make committed transactions equivalent to some serial order, using locks, validation, or serialization failures.`, `Name the invariant and anomaly before selecting isolation. Lost update may be prevented by write locks or version conditions; write skew across different rows requires a constraint, explicit locking of a common representative, or serializable validation. MVCC keeps row versions visible to different snapshots; vacuum or garbage collection can remove versions only after no relevant reader needs them.`, `Snapshot does not automatically mean serializable. Two transactions can each observe a condition and update disjoint rows so the combined invariant fails. Long snapshots increase version storage and replica lag. Read-only analytics can still obstruct maintenance. Engine names for isolation levels are not perfectly interchangeable; verify exact version behavior.`, `Protect the invariant that every on-call team always has at least one active administrator.`, `A transaction that merely counts active administrators then deactivates its own row can write-skew with another administrator doing the same. Enforce the invariant with a model that has a lockable team guard row, a deferred database constraint if the engine can express it, or serializable transactions with complete retry. Lock the guard before checking and updating, keep membership scope correct, and test simultaneous removal, role transfer, account deletion, and retry. Document why a lower isolation level is or is not safe.`, qs('transaction isolation', ['Isolation must be selected from the anomaly and invariant, not from its label alone', 'MVCC serves snapshots through row versions whose cleanup depends on active readers', 'Snapshot transactions can write skew when concurrent changes touch disjoint rows', 'Mastery constructs interleavings and demonstrates the exact constraint, lock, or retry that prevents them'])),
    lesson('schema-evolution', 'Schema design evolution and online migrations', `A schema evolves while data, old application versions, replicas, jobs, exports, and backups still exist. Safe change separates compatibility from cleanup. Additive nullable columns or new tables usually coexist; renames, type changes, new required constraints, and destructive drops need staged transitions. Migration correctness includes both schema and transformation of pre-existing rows.`, `Use expand, migrate, contract: add compatible shape, deploy code that reads old and new and writes the needed representation, backfill in bounded resumable chunks, validate counts and invariants, enforce the new constraint, switch all readers, wait through rollback and retention windows, then remove old shape later. Record migration identity, progress, duration, locks, and recovery.`, `One huge update can exhaust log, block traffic, lag replicas, and make rollback impossible. A default expression may rewrite a table in some engines. Creating an index can lock writes unless the online mode and failure semantics are understood. Application rollback fails if a destructive schema step already ran. ORM-generated migration review is still required.`, `Migrate a required free-text status column to a referenced status table with no service outage.`, `Add status table and nullable status id, seed frozen mappings, and deploy dual-write code while readers prefer id then fall back to text. Backfill by stable primary-key chunks with rate and replica-lag controls, rejecting unknown legacy values into a review report. Validate every row maps exactly once and compare application results. Add reference and required constraints using the engine's low-lock sequence, switch reads to id, stop old writes, retain the old column through rollback window, then drop it in a later release. Test migration from a populated prior schema and interrupted resume.`, qs('schema migration', ['Online evolution is a compatibility protocol across schema, data, code, and rollback windows', 'Expand, dual operation, bounded backfill, validation, constraint, and delayed cleanup reduce risk', 'A syntactically valid migration can still block traffic, lag replicas, or strand old code', 'Mastery upgrades populated prior versions, interrupts each phase, rolls back safely, and proves invariants'])),
    lesson('partitioning-retention', 'Partitioning, lifecycle, and large-table operations', `Table partitioning divides one logical relation into physical children by range, list, hash, or engine-specific scheme. It can prune irrelevant data, isolate lifecycle operations, and improve maintenance, but it does not replace good indexes or automatically distribute load across machines. The partition key becomes an operational and constraint choice.`, `Partition only when table scale and access or retention justify complexity. Choose boundaries from dominant predicates and deletion windows, precreate future partitions, route and validate inserts, keep indexes and constraints synchronized, and monitor imbalance. Retention should detach or drop complete old partitions after legal, product, backup, and replication requirements are satisfied.`, `Too many partitions increase planning and catalog work. Queries missing the partition predicate scan many children. A hot current-time partition remains one hotspot. Global uniqueness and foreign keys may be restricted. Late data can enter the wrong default partition, and dropping old partitions can break audits or restores if policy was not applied consistently.`, `Partition a ten-year audit-event table with monthly retention, late arrivals, tenant queries, and legal holds.`, `Use event time range partitions only if most queries and retention use it, while indexing each partition by tenant plus time and event id. Create future months automatically and quarantine records outside supported bounds rather than silently losing them. Keep hold metadata that prevents detaching affected data, or copy held events into a governed hold store with preserved identity. Run retention from an approved cutoff, validate counts and backups, detach first for a recovery window, then delete. Test month boundaries, time zones, leap dates, late records, missing future partition, hold release, and replica behavior.`, qs('database partitioning', ['Partitioning is a physical lifecycle and pruning technique tied to an explicit key', 'Boundaries, future creation, late data, indexes, holds, and deletion need one operational policy', 'Many partitions or a hot current partition can add cost without distributing the real bottleneck', 'Mastery creates, routes, queries, retains, holds, restores, and repairs partitions over time'])),
    lesson('replication-ha', 'Replication, failover, and high availability', `Replication copies database changes to other nodes for failover, locality, read scale, or recovery objectives. Synchronous acknowledgement waits for configured replicas; asynchronous followers can lag and may lose recent acknowledged changes under some failovers. Physical replication copies storage changes, while logical replication transfers row or statement-level changes with different compatibility.`, `Define recovery point and recovery time objectives per dataset, then select topology and acknowledgement policy. Monitor byte and time lag, replay health, disk, slots or log retention, and application connection routing. Promote only through a controlled authority with fencing so the former writer cannot continue. Reconfigure clients and replicas, validate committed data, and rebuild redundancy promptly.`, `A replica is not a backup because deletion and corruption replicate. Read scaling can violate read-your-writes and send a user backward. Automatic failover without quorum or fencing can split brain. A healthy process may be unrecoverably behind. Synchronous mode can sacrifice availability when its required replica is unreachable.`, `Design and rehearse database failover for a regional service with read replicas and five-minute recovery objective.`, `Choose one writer under a consensus-backed or otherwise strongly fenced authority, place a promotion candidate in another failure zone, and document acknowledgement and possible loss. Route session-sensitive reads to the writer or use a version fence; allow bounded-stale analytics on replicas. Detect writer failure from multiple perspectives, stop or fence it, confirm candidate replay, promote once, update routing, validate invariants and recent commits, and establish new replicas. Rehearse network partition, lagged candidate, disk full, failed promotion, old writer return, and client retry storm while measuring actual recovery.`, qs('database replication', ['Replication serves selected availability and scale goals under explicit acknowledgement semantics', 'Safe promotion requires authority, fencing, replay validation, routing, and restored redundancy', 'Replicas repeat bad writes and can serve stale or lost state after failover', 'Mastery partitions, promotes, rejects split brain, validates commits, and rebuilds the topology'])),
    lesson('distributed-databases', 'Sharding and distributed database tradeoffs', `Sharding divides a dataset across independently served partitions. Hash keys balance many similar identities, ranges preserve locality but can hotspot, and directories allow custom placement with a metadata dependency. Distributed queries, transactions, indexes, uniqueness, and referential integrity become protocols across shards rather than local engine operations.`, `Select a shard key from write locality, read routing, tenant size, ordering, join patterns, growth, and movement. Give rows global stable identities without a central hot counter. Keep transactions within one shard where domain boundaries allow, and use sagas or durable workflows for cross-shard effects. Build routing versions and online movement through copy, change capture, validation, cutover, and cleanup.`, `A uniform hash does not solve a tenant larger than one shard. Secondary queries fan out, global uniqueness coordinates, and resharding competes with production traffic. Cross-shard two-phase commit can block under coordinator failure and does not remove application reconciliation. Eventual conflict resolution can lose business intent if it merely picks a timestamp.`, `Shard a global media-tracking service with very large organizations and cross-user recommendation analytics.`, `Default to organization as locality but subdivide exceptional tenants by a stable media or member dimension while keeping a routing directory. Transactions for one user's tracking route to one ownership shard; organization-wide operations run as resumable jobs with per-shard idempotency. Recommendations consume an event stream into an analytical store rather than fan-out request queries. Migrate a tenant by snapshot, ordered change capture, checksum and invariant comparison, short guarded cutover, version-aware retry, and delayed source cleanup. Test hot tenant, directory loss, duplicate changes, rollback, and partial job completion.`, qs('distributed databases', ['A shard key chooses locality, balance, routing, and the scope of affordable invariants', 'Cross-shard work needs stable identity, durable progress, idempotency, and reconciliation', 'Uniform hashing, global transactions, and fan-out queries can move rather than remove bottlenecks', 'Mastery routes, migrates, validates, rolls back, and operates skewed shards under partial failure'])),
    lesson('backup-recovery', 'Backup, restore, point-in-time recovery, and disaster drills', `A backup is an independent recoverable copy, not merely another live replica. Full, incremental, snapshot, and continuous log backups trade storage, transfer, and restore complexity. Recovery point objective limits acceptable data loss; recovery time objective limits acceptable service interruption. Consistency across database, object files, encryption keys, and external systems must be designed.`, `Automate backups under dedicated least privilege, encrypt with separately recoverable keys, write to protected immutable or versioned storage, retain by policy, and monitor age and completion. A restore drill starts from empty infrastructure, retrieves keys and dependencies, restores a base plus logs to a chosen time, validates application invariants, and records achieved time and loss.`, `A green backup job can contain corrupt, incomplete, or un-restorable data. Snapshots across volumes may not share one transaction point. Point-in-time recovery after a malicious write must stop before the event without reintroducing it from queues. Restoring production into a test environment can violate privacy. Key loss makes perfect ciphertext unrecoverable.`, `Recover from an accidental tenant deletion discovered forty minutes later while other tenants continued writing.`, `Freeze further destructive automation, preserve evidence, identify the deletion transaction and affected dependencies, and choose a recovery point. Restore the full database to an isolated secured environment at just before deletion, export only the affected tenant with stable identities, and reconcile events that legitimately occurred afterward. Reinsert through a reviewed idempotent repair transaction while respecting unique keys and external files, then validate row counts, checksums, ownership, application journeys, audit continuity, and no cross-tenant overwrite. Record actual data gap and time, and add guarded deletion plus tested selective recovery tooling.`, qs('database recovery', ['Backups are valuable only when independent keys, artifacts, procedures, and restores succeed', 'Recovery objectives and drills turn stored copies into an operated business capability', 'Replication, job success, and snapshots alone cannot prove consistent recoverability', 'Mastery restores into isolation, validates invariants, reconciles later events, and documents achieved loss and time'])),
    lesson('observability-tuning', 'Database observability, capacity, and safe tuning', `Database health is user outcome plus workload, waits, and resources. Observe transaction rate, query latency distributions, errors, connections, queueing, locks, deadlocks, cache, disk latency and capacity, log generation, checkpoints, vacuum or compaction, replica lag, and backup age. Attribute work by bounded query fingerprint, application, role, and release.`, `Set service indicators for successful correct database operations by latency and freshness. Capture slow and high-total-cost queries, retain plan history safely, and establish workload baselines. Capacity includes peak traffic, maintenance, failover, backfill, restore, and growth headroom. Change one tunable with a hypothesis, test under representative concurrency, record before and after, and keep rollback.`, `Average latency hides tail and lock convoys. Connection pools larger than database capacity amplify queueing. Cache-hit ratio can be high while a small set of misses dominates harm. Increasing memory, disabling durability, or extending timeouts can mask a bad query while increasing crash or overload cost. High-cardinality query text and parameters leak data and overwhelm telemetry.`, `Investigate rising checkout latency after a release when database CPU remains moderate.`, `Trace the user operation and inspect pool wait, active sessions, locks, query fingerprints, plans, I/O, and downstream transaction duration. A new service call inside an open transaction may hold locks while CPU stays low; an N-plus-one path may exhaust the pool. Reproduce with release-matched load, fix transaction scope or query shape, cap concurrency, and keep timeouts below the user budget. Compare tail latency and throughput, not just CPU, then canary with pool saturation, lock wait, error, and business-success abort thresholds.`, qs('database operations', ['Database observability links user operations to queries, waits, resources, maintenance, and release', 'Capacity plans include concurrency, failure, migration, backup, and recovery work', 'Moderate CPU or high cache hits can coexist with lock, pool, I/O, and correctness failures', 'Mastery forms a measured hypothesis, changes safely, canaries, and retains rollback and evidence'])),
    lesson('security-governance', 'Database security, privacy, and data governance', `Database security protects confidentiality, integrity, availability, and accountable use across identities, network, engine, backups, exports, analytics, and administrators. Application authentication does not replace database identity and authorization. Data governance states why each field exists, its owner, classification, lawful or product basis, location, retention, deletion, and approved consumers.`, `Give applications, migrations, backups, analysts, and responders separate least-privileged roles with short-lived credentials where possible. Restrict network paths, verify encrypted transport, parameterize values, allowlist dynamic identifiers, protect keys separately, audit sensitive reads and privilege change, and mask or synthesize nonproduction data. Enforce tenant scope in queries and, where supported, add row policies as defense in depth.`, `Encryption at rest does not stop an authorized query from exfiltrating data. Shared administrator accounts erase accountability. Database logs and replicas may retain deleted personal data. Row policies can be bypassed by privileged owners or misconfigured session context. Backups, exports, caches, search indexes, and derived analytics must follow deletion and retention rules.`, `Design access and deletion for private libraries, support investigation, analytics, backups, and account closure.`, `Classify library and activity as private, use tenant-scoped application roles, and give support no default content access. A time-bound approved support grant records purpose and every read. Analytics receives minimized pseudonymous events with separate key and retention. Account closure requires recent authentication, creates a durable deletion workflow, revokes sessions, deletes or anonymizes relational and derived data, removes exported files, and records non-sensitive proof. Backups expire under a documented window and restored backups replay deletion tombstones before service. Test guessed tenant ids, leaked credentials, support expiry, log redaction, restore, and deletion reconciliation.`, qs('database security and governance', ['Database protection spans identities, queries, administrators, replicas, backups, and derived data', 'Least privilege and data lifecycle rules require separate owners, evidence, and revocation paths', 'Encryption or row policy alone cannot prevent privileged misuse and downstream retention', 'Mastery traces one sensitive field from collection through every copy, access, recovery, and deletion'])),
    lesson('capstone-database', 'Capstone: design and operate a production data platform', `Build the persistence layer for a multi-organization media service. It stores identities, memberships, titles, tracking, immutable events, background jobs, audit, and exports. Produce a data dictionary, row grains, keys, constraints, query catalog, authorization matrix, retention map, recovery objectives, and capacity model before selecting physical structures.`, `Implement versioned schema and realistic seed generator, parameterized query layer, transactions for membership and tracking invariants, cursor APIs, idempotency and outbox, workload-derived indexes, online migration from a populated prior version, monthly event lifecycle, one read replica or faithful simulation, encrypted automated backup, telemetry, and least-privileged roles. Keep one reproducible artifact and configuration history.`, `Exercise concurrent updates, write skew, deadlock, skewed tenant, stale statistics, plan regression, disk pressure, pool exhaustion, migration interruption, replica lag, writer partition, corrupt page or fixture, accidental deletion, leaked credential, expired backup key, retention hold, and privacy deletion after restore. A dashboard without repair is incomplete; every alert links to a rehearsed action and owner.`, `Create a traceability and recovery book that proves each data promise from schema through operations.`, `For every invariant list grain, constraint, transaction, isolation, race test, query plan, metric, alert, and repair. For every dataset list source, owner, classification, reader, copy, retention, backup, and deletion. Record baseline workload and capacity, migration checkpoints and rollback, failover fencing and validation, and restore commands with expected checksums. The platform passes when another operator can load it, explain every plan, interrupt migrations, fail over without split brain, restore to a selected point, repair one tenant, and prove no cross-tenant access using only included material.`, qs('database mastery', ['Database mastery connects logical facts, physical access, concurrency, operations, and governance', 'Every data promise needs a durable mechanism, adversarial test, signal, owner, and repair path', 'Schema creation and green queries alone cannot prove migration, failover, privacy, or recovery', 'Mastery models, loads, races, tunes, migrates, partitions, fails over, restores, secures, and explains the platform']))
  ]
}
