import type { ProgCourseDef } from './types'
import { masteryLesson as lesson, masteryQuestions as qs } from './masteryCourse'

export const BACKEND_DISTRIBUTED_COURSE: ProgCourseDef = {
  key: 'backend-distributed-systems',
  title: 'Backend and distributed systems: beginner to master',
  description:
    'A self-contained path through backend contracts, concurrency, transactions, messaging, caching, replication, consensus, resilience, performance, and distributed-system capstones.',
  lessons: [
    lesson('backend-contracts', 'Backend boundaries and service contracts', `A backend turns authenticated requests or events into durable business outcomes. Separate transport, domain rules, persistence, and external integrations. State the grain and invariant of every entity, the authority for every decision, and the lifecycle of every operation. A successful response is a contract about what became true, not merely that code ran.`, `Define inputs, runtime validation, authorization, transaction boundary, idempotency, output, error categories, timeout, retry, observability, and compatibility for each operation. Use ports around databases, clocks, queues, and remote clients so pure decisions are testable. Keep indispensable state durable and reconstructable across replicas and restarts.`, `Framework conventions can hide transactions, global state, implicit retries, and error translation. Do not leak database rows directly as API contracts. A health check cannot prove business correctness. Model partial failure and unknown outcomes before coding, and expose enough identity to reconcile them.`, `Specify the complete contract for accepting and processing one background job.`, `Authenticate and authorize acceptance, validate a bounded payload, require an idempotency key, insert the job and outbox event transactionally, and return a stable job identity. Workers claim by lease, commit one terminal state conditionally, and can retry without duplicate effects. Polling exposes status and bounded errors. Logs and metrics carry operation identity, while cancellation, retention, and recovery are explicit.`, qs('backend contracts', ['A backend operation promises a durable domain outcome rather than mere code execution', 'Transport, domain, persistence, and integration boundaries need explicit interfaces', 'Unknown outcomes require stable identity and reconciliation instead of blind retry', 'Mastery specifies success, failure, authority, timing, evidence, and recovery end to end'])),
    lesson('api-design', 'HTTP, RPC, and API evolution', `HTTP APIs use methods, resource identifiers, status codes, headers, representations, caching, and conditional requests. RPC APIs express procedures with typed messages. Neither style guarantees good design. Model capabilities from domain semantics, choose idempotency and pagination, and keep errors stable and actionable.`, `GET is safe, PUT replaces a named resource idempotently, PATCH applies a defined partial change, and POST commonly creates or triggers work. Use ETags or versions for conditional mutation. Cursor pagination survives inserts better than offset when ordering is stable. Define deadlines and propagate a remaining budget downstream.`, `Retries can duplicate non-idempotent operations; status 500 does not prove nothing committed. Version through backward-compatible additions, tolerant readers where safe, and explicit deprecation. Validate content type and size. Never use random ids as authorization or return internal stack traces.`, `Design create, update, list, and delete contracts for media lists with concurrent editors.`, `Create with a client idempotency key and return 201 plus location. List by stable cursor ordered on a unique pair. Update requires the current version in If-Match and returns conflict on stale edits. Delete is idempotent and tombstones when restoration or event ordering needs it. Every operation scopes organization and role, emits a stable error code, and preserves compatibility through additive fields.`, qs('API design', ['API semantics define business meaning, idempotency, compatibility, and recovery', 'Conditional versions prevent silent lost updates between concurrent editors', 'A timeout or server error can leave mutation outcome unknown to the client', 'Mastery designs methods, messages, errors, pagination, deadlines, and evolution together'])),
    lesson('domain-data', 'Domain models, invariants, and persistence', `A domain model names entities, values, events, policies, and invariants in the language of the problem. Persistence models optimize storage and queries; transport models optimize compatibility. They may differ. Define aggregate boundaries around invariants that must commit atomically rather than mirroring every object relationship into one giant transaction.`, `Use database constraints for durable uniqueness, references, ranges, and required fields. Domain services coordinate rules spanning entities. Repositories expose intention-revealing queries already scoped to ownership. Events describe facts after commit and carry stable identity, schema version, actor, and occurrence time.`, `An anemic model scatters rules across controllers and jobs. An overgrown aggregate serializes unrelated work. Object-relational mapping can cause hidden queries and weak boundaries. Historical facts should not be rewritten like current state; audit needs event or version semantics and privacy-aware retention.`, `Model subscriptions, plan changes, invoices, and access without relying on current plan for history.`, `Represent account and current subscription separately from immutable invoice facts. Plan changes have effective intervals or events; access checks use the version valid now, while invoices store price, currency, tax, and plan snapshot used at creation. Constraints prevent overlapping active intervals and duplicate invoice periods. A transaction changes subscription and records an outbox event. Tests cover concurrent change, late event, cancellation, and historical reads.`, qs('domain modeling', ['Domain models preserve business language and invariants independently of transport shape', 'Database constraints and aggregate boundaries place rules at durable concurrency points', 'Current mutable state cannot safely reconstruct every historical business fact', 'Mastery aligns entities, transactions, events, queries, history, and ownership'])),
    lesson('concurrency', 'Concurrency, synchronization, and backpressure', `Concurrency means operations overlap; parallelism means work executes simultaneously. Threads, async tasks, processes, and distributed workers all create interleavings. A race occurs when correctness depends on timing. Identify shared mutable state, invariant, critical section, and ownership before choosing a mutex, channel, transaction, actor, or immutable message.`, `Locks protect critical sections but need ordering, bounded duration, and cancellation policy. Optimistic concurrency detects version change at commit and retries the whole decision. Queues transfer ownership and provide backpressure. Semaphores bound scarce work. Avoid holding locks across remote calls.`, `Deadlock, livelock, starvation, priority inversion, unbounded queues, and thread-pool exhaustion are distinct failures. Async code can still race. Retrying under contention can amplify load; add jitter and budgets. Test with deterministic barriers and high repetition, but use invariants rather than hoping a race appears.`, `Protect a limited inventory count from simultaneous purchases across replicas.`, `Keep inventory in one transactional store. Update with a conditional statement that decrements only when remaining quantity is sufficient and insert an order under a unique idempotency key in the same transaction. If zero rows update, return sold out. Do not read then write without protection. Bound database concurrency, avoid global process locks, and test hundreds of synchronized attempts to prove successful quantity never exceeds stock.`, qs('concurrency control', ['Correctness depends on shared-state invariants, not on expected operation timing', 'Choose synchronization from ownership and atomicity rather than habit', 'Unbounded concurrency and retries can turn contention into system-wide collapse', 'Mastery proves invariants under controlled interleavings, cancellation, and overload'])),
    lesson('transactions-idempotency', 'Transactions, idempotency, and outbox patterns', `A transaction groups changes under atomicity and isolation guarantees provided by one store. Isolation levels permit different anomalies; understand the engine rather than treating ACID as one universal behavior. Idempotency means repeating a logical operation converges on one business effect, usually through a stable key and stored result.`, `Store the idempotency key with request identity, normalized operation scope, status, and response inside the transaction that commits the effect. For database plus message publication, write an outbox row in the same transaction, then publish asynchronously and mark progress. Consumers deduplicate by event identity and make their own effects idempotent.`, `Distributed transactions add coordination and availability costs and still require operational recovery. Exactly-once delivery claims are scoped; networks can duplicate attempts and acknowledgements can be lost. Do not keep idempotency records for less time than clients may retry. A key reused for different payload must be rejected.`, `Design a payment-like charge followed by an email and analytics event.`, `Accept a client operation id, validate it against account and payload hash, and transactionally create one charge record plus outbox events. Return the stored result on retry. A publisher retries stable event ids. Email records a send decision by event id before the provider call and reconciles unknown outcomes; analytics upserts by event id. No email or analytics failure rolls back the charge. Retention exceeds retry policy and reconciliation dashboards expose stuck states.`, qs('transactional idempotency', ['Idempotency binds repeated attempts to one stored logical operation', 'An outbox atomically records domain change and intent to publish', 'Exactly-once business effects require identity and deduplication beyond transport claims', 'Mastery handles commit, publish, consume, unknown acknowledgement, retention, and repair'])),
    lesson('queues-events', 'Queues, event streams, and delivery semantics', `A queue buffers work and transfers ownership; an event stream preserves ordered records for consumers. Topics, partitions, consumer groups, offsets, visibility timeouts, and dead-letter handling implement different semantics. Choose from ordering, throughput, retention, replay, fan-out, latency, and failure recovery, not fashion.`, `Partition by the identity whose order must be preserved. Messages carry immutable id, schema version, occurrence time, producer, trace context, and bounded payload or reference. Consumers checkpoint only after durable effect. Poison messages need limited attempts, classified failure, quarantine, inspection, and replay after correction.`, `Global order restricts throughput. Per-partition order is lost if related entities use different keys. Queue depth alone hides old work; measure oldest age and completion rate. Dead-letter queues can become graveyards. Backlog replay can overload dependencies, so throttle from downstream health.`, `Design an order-event stream serving billing, inventory, email, and analytics.`, `Publish immutable order events from an outbox keyed by order id so changes for one order stay ordered. Each consumer group owns offsets and idempotent local effects. Billing and inventory use strict schemas and quarantine incompatible events; email derives notifications without blocking order state; analytics tolerates replay by event-id upsert. Monitor lag in time, retries, poison counts, and dependency saturation. Version additively and rehearse full replay at a safe rate.`, qs('messaging systems', ['Queues and streams trade ordering, replay, fan-out, and ownership in different ways', 'Partition keys should preserve the smallest business order that actually matters', 'Backlog, poison messages, and replay require bounded operational workflows', 'Mastery proves publish, consume, deduplicate, quarantine, replay, and schema evolution'])),
    lesson('caching', 'Caching, invalidation, and derived state', `A cache stores derived or copied data to reduce latency or work. Name its source of truth, key, value, freshness, maximum staleness, invalidation, capacity, eviction, security scope, warm-up, and failure behavior. Browser, CDN, application, database, and local caches operate at different trust and consistency boundaries.`, `Cache-aside reads source on miss and fills; write-through updates cache with source; write-behind defers source updates and increases durability risk. Versioned keys make schema or permission changes explicit. Use request coalescing and jittered expiry to reduce stampedes. Negative caching needs short policy because absence changes.`, `Invalidation is a distributed update. TTL limits staleness but does not guarantee fresh authorization. Cache keys missing tenant, role, locale, encoding, or query inputs can disclose wrong responses. Hot keys and synchronized expiry overload stores. Treat cache loss as performance degradation unless the cache is intentionally a durable system.`, `Cache private media details and public artwork safely across application and CDN layers.`, `Application keys include tenant, media id, representation version, and relevant permission version; short freshness plus event invalidation updates details. Never cache an authorized response under media id alone. Public immutable artwork uses content digests and long CDN caching, while mutable aliases revalidate. Coalesce misses, cap object size, jitter expiry, and serve source on cache failure. Monitor hit ratio with latency and source load rather than optimizing hits alone.`, qs('cache design', ['A cache is derived state with declared truth, key, freshness, and failure behavior', 'Security and representation dimensions must participate in cache identity', 'TTL, eviction, and invalidation can create stale data and synchronized load', 'Mastery proves correct hits, misses, expiry, permission change, stampede, and cache loss'])),
    lesson('service-boundaries', 'Service boundaries and evolutionary architecture', `A service boundary groups data, rules, ownership, and change cadence behind an interface. Split where independent capability and team ownership justify network and operational cost, not by database table or organizational chart alone. A modular monolith can enforce strong boundaries without distributed failure and is often the best starting point.`, `Map business capabilities, transactions, dependencies, change patterns, scaling needs, and failure isolation. One service owns a piece of mutable data; others use its API or events rather than sharing tables. Contracts include SLO, schema, idempotency, authentication, compatibility, and support.`, `Microservices add latency, partial failure, deployment coordination, tracing, eventual consistency, security boundaries, and on-call load. Chatty calls reveal a poor boundary. Shared libraries can couple releases as tightly as shared databases. Avoid cyclic synchronous dependencies and distributed monoliths that must deploy together.`, `Decide whether catalog, ratings, recommendations, and identity should be separate services for a small product.`, `Begin with modules and one deployable application unless teams, scaling, regulation, or failure evidence demand separation. Identity is a clear security capability but can remain a module or managed provider. Catalog and ratings may share user journeys but keep data ownership explicit. Recommendations can consume events asynchronously and scale separately when workload grows. Record triggers such as independent team ownership, materially different scale, or required isolation, then extract through a stable interface and data migration rather than speculative splitting.`, qs('service boundaries', ['A service boundary should reflect capability, ownership, and independent lifecycle', 'Single ownership of mutable data avoids competing cross-service truth', 'Premature network boundaries create latency, consistency, security, and operational costs', 'Mastery can defend both a modular monolith and a justified extraction path'])),
    lesson('time-order', 'Time, ordering, identifiers, and distributed truth', `Distributed machines do not share a perfectly synchronized clock. Wall time can jump and differs by node; monotonic time measures duration within one process. Event time is when a fact occurred, processing time is when a system handled it, and ingestion time is when it arrived. Keep meanings separate.`, `Use sortable or random unique identifiers according to privacy and ordering needs, but do not infer causality from timestamps alone. Sequence numbers, versions, logical clocks, and vector clocks represent selected ordering. Lamport clocks establish a consistent happens-before-compatible order but cannot identify all concurrency; vectors can at greater metadata cost.`, `Last-write-wins can discard valid concurrent changes and clock skew can choose the wrong winner. Global counters and total order require coordination. Duplicate ids, replay, late events, and daylight-saving conversions break naive logic. Store instants in UTC and preserve local civil rules when future schedules depend on them.`, `Merge offline edits to a watchlist where users add, remove, and reorder items on multiple devices.`, `Give operations unique ids, device identity, logical sequence, and observed base version. Model add and remove with explicit item identity and tombstones; do not use wall-clock last-write-wins. Reorder can use position identifiers or a conflict policy surfaced to users. Server deduplicates operations, preserves causal metadata, and compacts tombstones only after every supported replica horizon. Test simultaneous add/remove, repeated delivery, clock skew, offline months, and account deletion.`, qs('distributed time and order', ['Wall clocks do not establish complete causality across independent machines', 'Operation identity and logical versions make replay and concurrency explicit', 'Last-write-wins and global ordering can silently discard data or require coordination', 'Mastery distinguishes event time, processing time, duration, causality, and conflict policy'])),
    lesson('replication-consistency', 'Replication, consistency, and CAP tradeoffs', `Replication copies state for availability, locality, read scale, or durability. Synchronous replication waits for selected replicas before success; asynchronous replication reduces latency but can lose recent acknowledged state on failover depending on the contract. Leader-based, multi-leader, and leaderless designs resolve writes differently.`, `Define consistency per operation: linearizable read, read-your-writes, monotonic reads, causal consistency, bounded staleness, or eventual convergence. Quorums work only under assumptions about replica sets, failures, clocks, and conflict resolution. CAP says that during a network partition a distributed system cannot guarantee both linearizable availability and every request succeeding.`, `Replication is not backup because bad writes replicate. Read replicas may be stale and clients can jump backward between them. Failover can create split brain or lost acknowledged writes when fencing is weak. Consistency marketing terms are insufficient; test the exact version and configuration.`, `Choose consistency for username registration, media-view counts, and a user's just-edited profile.`, `Username uniqueness needs one serialized authority or strongly consistent conditional write. View counts can accept partitioned increments and eventual aggregation with deduplicated events. A profile update should provide read-your-writes to that user through leader reads, session stickiness, or version-aware routing, while public profile propagation may be bounded-stale. Document failure behavior: during partition registration may reject rather than duplicate, counts queue, and profile reads show a version or pending state.`, qs('replication consistency', ['Replication improves selected availability or scale but has explicit acknowledgement semantics', 'Consistency should be chosen per user operation rather than one vague system label', 'Replicas can repeat bad writes, serve stale data, or split without correct fencing', 'Mastery explains partition behavior, acknowledged loss, conflict, failover, and user guarantees'])),
    lesson('consensus-coordination', 'Consensus, leases, and distributed coordination', `Consensus lets a group agree on an ordered log or value despite some failures, under protocol assumptions. Systems such as Raft elect a leader, replicate log entries to a majority, and commit entries safe under terms and quorum. Consensus does not make application commands correct or provide availability without a quorum.`, `Use consensus-backed stores for small critical coordination metadata: membership, leader election, configuration, or locks. A lease grants time-bounded authority and requires a trustworthy expiry model. Fencing tokens are monotonically increasing values checked by the protected resource so an expired holder cannot continue writing after a new owner appears.`, `A distributed lock without fencing is unsafe when a paused client resumes. Holding consensus transactions across slow work harms availability. Membership changes and snapshot restoration are operationally sensitive. Do not use the coordination store as a high-volume event database merely because it is consistent.`, `Design single-active scheduler ownership that remains safe when a process pauses beyond its lease.`, `Acquire a lease from a consensus-backed authority and receive a monotonically increasing fencing token. Include the token in every mutation to the scheduled-job store, which rejects tokens older than the highest seen. Renew within a margin but stop admission when renewal is uncertain. A new scheduler obtains a greater token and can proceed; the paused old process cannot commit after resuming. Jobs still use stable ids and idempotency because scheduler failover can repeat attempts.`, qs('distributed consensus', ['Consensus establishes ordered agreement only while protocol quorum assumptions hold', 'Leases need fencing at the protected resource to stop stale holders', 'A lock name alone cannot prevent a paused former owner from writing later', 'Mastery separates coordinator authority, resource fencing, job idempotency, and recovery'])),
    lesson('partitioning-sharding', 'Partitioning, sharding, and data movement', `Partitioning divides data or work by a key. Hash partitioning balances random keys; range partitioning supports ordered scans but can create hot recent ranges; directory routing permits custom placement but adds metadata. Sharding distributes partitions across independent stores and makes cross-shard operations harder.`, `Choose a key from access patterns, locality, ordering, tenancy, load, and growth. Include virtual partitions so physical placement can change without redefining every key. Maintain a routing version, migrate through copy, change capture, validation, cutover, and cleanup, and make clients tolerate movement.`, `Skew, celebrity keys, monotonically increasing ids, uneven tenant sizes, cross-shard joins, global uniqueness, and secondary indexes defeat simple balance. Resharding uses network and disk and can amplify production load. Never hash a key if all requests for one giant tenant still land together without a subdivision strategy.`, `Partition a global activity stream for ordered per-user feeds and aggregate trends.`, `Key the durable event stream by user id so one user's events remain ordered, use many logical partitions, and distribute them across brokers. Very large users can subdivide by stable feed lane only if readers merge with explicit order. Analytics consumes all partitions independently and aggregates by time windows. Global event ids are unique without a central counter. Rebalancing preserves committed offsets, throttles movement, and validates counts and lag before removing old replicas.`, qs('partitioning and sharding', ['Partition keys encode locality, ordering, balance, and future movement tradeoffs', 'Logical partitions and routing versions make physical rebalancing more manageable', 'Skew and cross-shard invariants can dominate a theoretically even hash design', 'Mastery performs copy, capture, validate, cut over, observe, and clean up safely'])),
    lesson('resilience', 'Timeouts, retries, overload, and fault containment', `Distributed calls fail by refusal, timeout, reset, corruption, partial response, overload, or unknown outcome. Every call needs connect and operation deadlines derived from an end-to-end budget. Retries are additional load and belong only on transient errors for idempotent or deduplicated operations. Use exponential backoff, jitter, and a maximum elapsed budget.`, `Bulkheads isolate resource pools, circuit breakers stop repeated calls during sustained failure, queues absorb bounded bursts, load shedding protects essential work, and fallbacks preserve reduced value. Health and readiness control admission. Graceful shutdown drains. Apply backpressure as close to demand as possible and cap every queue.`, `Layered retries multiply attempts. Circuit breakers can synchronize oscillation. Fallback caches can serve unsafe stale authorization. Timeout shorter than normal tail latency creates self-inflicted failure. Capacity plans include one failure domain lost and dependency limits, not only average CPU.`, `Protect an API calling three dependencies when one becomes slow and another rate-limits retries.`, `Allocate a total request deadline and smaller per-call budgets, run independent calls concurrently under bounded pools, and cancel obsolete work. Retry only the documented transient idempotent dependency with jitter and one shared attempt budget, honoring server retry hints. Isolate pools so the slow dependency cannot consume every worker. Return partial optional data but fail closed for required authorization. Shed low-priority requests before saturation, expose dependency outcomes, and page on user harm rather than every timeout.`, qs('distributed resilience', ['Deadlines and retry semantics belong to the complete user operation budget', 'Bulkheads, backpressure, and shedding limit resource and failure propagation', 'Nested retries and unsafe fallbacks can amplify outages or violate authorization', 'Mastery demonstrates degraded service, containment, recovery, and bounded resource use'])),
    lesson('observability', 'Distributed observability and SLOs', `Metrics summarize rates and distributions, logs capture discrete context, and traces connect causal spans. Propagate request and operation identity across HTTP, messaging, jobs, and database work. Instrument user boundaries plus dependency calls, queues, retries, saturation, and version. Keep metric labels bounded.`, `Define SLIs from good events over valid events for availability, latency, freshness, or correctness. Set objectives and error-budget policy from user need. Use multi-window burn alerts for meaningful harm. A service map and trace are diagnostic aids, not substitutes for an owned runbook.`, `Sampling can miss rare failures, clocks can distort spans, and high-cardinality identifiers can bankrupt metrics. Logging complete payloads leaks data. A healthy server metric can coexist with failed clients. Monitor telemetry health and use external synthetics for the path users experience.`, `Create observability for an event that crosses API, outbox, broker, worker, and notification service.`, `Assign one operation id at acceptance and one event id per outbox fact. Propagate trace context through headers and message metadata. Measure accepted-to-terminal latency, success, duplicates, queue age, consumer lag, retry, and dead-letter by bounded type and outcome. Logs include ids, version, state transition, and safe error code. Trace samples retain failures. The SLI counts accepted operations reaching correct terminal state by deadline, and the runbook moves from burn alert to queue, trace, release, and dependency evidence.`, qs('distributed observability', ['Observability must connect user operations across process and message boundaries', 'SLIs measure user-relevant good events and drive an explicit error-budget response', 'Telemetry can lie through sampling, cardinality, missing paths, or sensitive overcollection', 'Mastery follows one operation from acceptance through every retry, state, and outcome'])),
    lesson('performance-security', 'Performance, capacity, and distributed security', `Performance starts with workload model: arrival rate, concurrency, request mix, data size, cache state, locality, and objective. Throughput, latency distribution, errors, saturation, and queue age reveal limits. Little's Law relates average concurrency, throughput, and time in a stable system. Profile the constrained resource before optimization.`, `Capacity includes headroom for bursts, rollout, failover, rebalancing, and recovery. Batch, cache, index, compress, or parallelize only with measured tradeoffs. Security uses authenticated workload identity, least privilege, protected transport, scoped authorization, network policy, secret rotation, and audit across every hop.`, `Benchmarking warm tiny data misleads. Coordinated omission hides periods when clients could not send. Adding replicas can overload a shared database. Service-to-service encryption without identity or authorization is incomplete. More telemetry and caching can expose sensitive data.`, `Plan a load and security review for a new high-volume recommendation endpoint.`, `Model peak and burst traffic, user and item distributions, cold and warm caches, expensive misses, one-zone loss, and downstream limits. Load with realistic arrival independent of response, measure end-user percentile, errors, queueing, CPU, memory, database, cache, and network. Authenticate user and workload, scope recommendation data, cap query and response, protect model and features, and audit unusual bulk access. Set cache keys by user and model version, load-shed optional explanations, and stop scale-out at safe dependency capacity.`, qs('backend performance and security', ['Performance conclusions require representative workload, distributions, and user objectives', 'Capacity reserves room for failure, deployment, recovery, and shared dependency limits', 'Scaling one tier or adding caches can overload dependencies or expose private data', 'Mastery combines profiling, load, identity, authorization, saturation, and safe degradation'])),
    lesson('capstone-distributed', 'Capstone: a resilient distributed job platform', `Build a local platform with API, relational database, outbox publisher, partitioned broker or simulation, workers, notification consumer, cache, and observability. Clients submit jobs idempotently, query state, cancel, and receive notifications. Define user guarantees, data ownership, message schemas, consistency, deadlines, and threat model before implementation.`, `Implement conditional state transitions, transactional outbox, at-least-once messages, deduplicated consumers, leases with fencing where needed, partition order, retry budgets, poison quarantine, cursor APIs, tenant authorization, short-lived service identity, and graceful drain. One artifact and versioned schema move through a reproducible delivery path.`, `Inject duplicate delivery, lost acknowledgement, broker pause, stale replica, clock skew, worker partition, poison message, hot key, database failover, cache loss, expired credential, and overloaded notification provider. Rebuild read models from events, restore the database, reconcile unknown outcomes, and verify no job completes twice or crosses tenants.`, `Write an invariant and failure matrix covering every boundary and the evidence that proves recovery.`, `For each operation record source of truth, stable identity, allowed states, transaction, emitted event, consumer effect, timeout, retry, deduplication, ordering, and reconciliation. Map failures before and after commit and acknowledgement. Attach metrics, logs, traces, alert, owner, and runbook. The platform passes when another person can replay history, kill any one component, observe bounded degradation, recover from backup, and prove job and tenant invariants without relying on timing.`, qs('distributed-systems mastery', ['A distributed design begins with invariants, ownership, timing, and failure contracts', 'Stable identity and durable state transitions make retries and replay convergent', 'Component uptime alone cannot prove end-to-end correctness under partial failure', 'Mastery builds, partitions, overloads, restores, reconciles, and explains the complete system']))
  ]
}
